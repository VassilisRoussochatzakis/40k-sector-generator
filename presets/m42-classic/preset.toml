title = "M42 Classic"
description = "Balanced canonical Imperium sector — the default M42 setup, good for first-time users. Fully fleshed out: every overlay (factions, relations, regions, economy, history, personae, sites, hooks, missions, prose) and every output format (incl. SVG + interactive HTML) is enabled. The general-purpose baseline for fully-random generation."
tags = ["sample", "balanced", "imperium", "fully-featured", "random-baseline"]
default_seed = "m42-default-seed"
inherits = "_base"
