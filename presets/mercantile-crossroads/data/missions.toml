# Mercantile Crossroads — mission/quest seed knobs (§3 NEW2.md / §M1..§M5 BUILDER_REQS).
#
# Parsed directly into `MissionsConfig` (bare file — no `[missions]` wrapper).
# Player-facing edition on — a trade sector is built for Rogue Trader crews
# chasing charters, cargoes, and contacts.

max_per_anchor = 3
top_n_digest = 18
player_edition = true
