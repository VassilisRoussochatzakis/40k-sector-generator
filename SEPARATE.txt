# SEPARATE.txt
# Instructions: split the in-GUI builder (gui/src/builder/) into its own
# binary crate `sectorforge-builder`, leaving `sectorforge-gui` as the
# viewer/editor. Author: prior conversation, 2026-05-24.
#
# These instructions are written for a future Claude Code run. Read top to
# bottom before doing anything. Verify state hasn't drifted (Section 2)
# before executing. Do NOT skip the verification anchors.
#
# Implementation status (Codex, 2026-05-24):
#   - Section 1 pre-flight: DONE
#   - Step 3.1 shared crate skeleton: DONE
#   - Step 3.2 move shared widgets: IN-PROGRESS

================================================================================
0. WHY (so you can make judgment calls)
================================================================================

Today the builder ships inside `sectorforge-gui` under `gui/src/builder/`
(~8.1k lines, 37% of the GUI crate). Coupling to the rest of the GUI is
*currently* exactly 2 symbols:
  - `gui/src/builder/preview.rs`        uses `crate::jobs::{spawn_job, JobHandle}`
  - `gui/src/builder/panels/map.rs`     uses `crate::palette::{...}`

The viewer `App` struct (`gui/src/app/mod.rs`) holds ZERO builder state. No
non-builder code in `gui/src/` references `BuilderState`, `BuilderWorkspace`,
or any `crate::builder::*` symbol. `gui/src/lib.rs:6` is the only top-level
wire-in (`pub mod builder;`).

BUILDER_REQS.txt §G6 / Phase B host wiring is NOT yet landed. Once it lands
(workspace tab strip + builder panels mounted into App), coupling explodes
and this split becomes expensive. Split NOW.

Goal: three crates after this work:
  1. `sectorforge`             (existing lib + CLI bin, unchanged)
  2. `sectorforge-gui-core`    (NEW shared widget/util crate)
  3. `sectorforge-gui`         (existing crate, viewer/editor only, slimmed)
  4. `sectorforge-builder`     (NEW crate, owns builder + thin App)

Communication path between viewer and builder: project-directory file
handoff. Builder saves a project; viewer is launched with
`sectorforge-gui --project <dir>` and auto-reloads via the existing
FileWatcher / `load_sector_json` path. No IPC, no shared memory.

================================================================================
1. PRE-FLIGHT — RE-VERIFY STATE BEFORE TOUCHING ANYTHING
================================================================================

The numbers below were captured on 2026-05-24. If they have drifted
materially, STOP and re-evaluate before proceeding — drift may mean Phase B
wiring has landed and the split plan needs revision.

Run each check. Expected results in [brackets].

  1.1  `git status`
       [Expected: clean working tree. If dirty, ask user to stash/commit.]

  1.2  `git log --oneline -5`
       [Expected: builder reqs commits. Note the head SHA for rollback.]

  1.3  Count current builder code:
         find gui/src/builder -type f -name "*.rs" -exec wc -l {} + | tail -1
       [Expected: ~8,100 lines. If >12k, Phase B has landed — re-plan.]

  1.4  Verify coupling is still minimal:
         grep -rhn "^use crate::" gui/src/builder --include="*.rs" \
           | grep -v "use crate::builder" | sort -u
       [Expected: exactly two distinct lines, importing `crate::jobs` and
        `crate::palette`. If more, list them and re-scope the shared crate.]

  1.5  Verify viewer doesn't reference builder:
         grep -rn "BuilderState\|BuilderWorkspace\|crate::builder\|sectorforge_gui::builder" \
           gui/src/app gui/src/editor gui/src/lib.rs gui/src/main.rs \
           gui/src/dashboard.rs gui/src/factions_overview.rs \
           gui/src/preset_gallery.rs gui/src/route_planner.rs \
           gui/src/segmentum_view.rs gui/src/data_editor.rs \
           gui/src/sector_view.rs gui/src/system_view.rs \
           gui/src/info_panel.rs gui/src/heatmap.rs gui/src/jobs.rs \
           gui/src/palette.rs --include="*.rs"
       [Expected: NO matches. If matches, list them. Each one is extra work.]

  1.6  Baseline build + test:
         cargo check --workspace
         cargo test --workspace --no-run
       [Expected: both succeed. Capture warning count for diff later.]

If 1.4 or 1.5 fail, do NOT proceed with the mechanical plan. Report findings
to the user and ask whether to (a) absorb the extra coupling into the
shared crate, or (b) abort the split.

================================================================================
2. TARGET LAYOUT
================================================================================

Workspace `Cargo.toml`:
  [workspace]
  members = ["gui", "gui-core", "builder"]

Repo tree (additions only):
  gui-core/
    Cargo.toml                # name = "sectorforge-gui-core"
    src/
      lib.rs
      palette.rs              # moved from gui/src/palette.rs
      jobs.rs                 # moved from gui/src/jobs.rs
      sector_view.rs          # moved from gui/src/sector_view.rs (read-only widget)
      system_view.rs          # moved from gui/src/system_view.rs
      info_panel.rs           # moved from gui/src/info_panel.rs
      heatmap.rs              # moved from gui/src/heatmap.rs
  builder/
    Cargo.toml                # name = "sectorforge-builder"
    src/
      main.rs                 # NEW: bin entry, mirrors gui/src/main.rs shape
      lib.rs                  # NEW: pub use of builder + thin App
      app.rs                  # NEW: builder-only eframe::App host
      builder/                # MOVED wholesale from gui/src/builder/
        mod.rs
        command.rs
        ...
        panels/
          ...

`gui/Cargo.toml` gains `sectorforge-gui-core = { path = "../gui-core" }` and
drops nothing (still depends on `sectorforge`, `eframe`, `egui`, `rfd`, etc.).

`builder/Cargo.toml` deps: `sectorforge`, `sectorforge-gui-core`, `eframe`,
`egui`, `rfd`, `serde`, `serde_json`, `toml`, `camino`, `clap`, `tempfile`,
`thiserror`, plus anything the existing `gui/src/builder/` already pulls
(e.g. `image`, `include_dir` if used by panels — check by grepping
`gui/src/builder` for `use image::` / `use include_dir::`).

================================================================================
3. EXECUTION — STEP BY STEP
================================================================================

Use `git mv` for moves so history follows. Commit after each numbered step
that successfully `cargo check`s, so rollback is per-step. Do NOT batch.

Step 3.1  Create the shared crate skeleton
----------------------------------------------------------------------
  mkdir gui-core gui-core/src
  Create gui-core/Cargo.toml:
    [package]
    name = "sectorforge-gui-core"
    version = "0.1.0"
    edition = "2021"

    [dependencies]
    sectorforge = { package = "sector-generator", path = ".." }
    eframe = { version = "0.29", default-features = false, features = ["default_fonts", "glow", "wayland", "x11"] }
    egui = "0.29"
    serde = { version = "1", features = ["derive", "rc"] }
    serde_json = "1"
    image = { version = "0.25", default-features = false, features = ["png"] }
    # add others only if a moved file actually needs them — check imports

  Create gui-core/src/lib.rs:
    pub mod palette;
    pub mod jobs;
    pub mod sector_view;
    pub mod system_view;
    pub mod info_panel;
    pub mod heatmap;

  Add to root `Cargo.toml` `[workspace] members`: `"gui-core"`.

  cargo check -p sectorforge-gui-core
  [Expected: succeeds — empty modules.]

Step 3.2  Move shared widgets into gui-core
----------------------------------------------------------------------
  For each file in this list, `git mv` it from `gui/src/` to `gui-core/src/`:
    palette.rs
    jobs.rs
    sector_view.rs
    system_view.rs
    info_panel.rs
    heatmap.rs

  Then in `gui/src/lib.rs`:
    - Remove the corresponding `pub mod palette;` / `pub mod jobs;` / etc.
    - Add at the top:
        pub use sectorforge_gui_core::{palette, jobs, sector_view, system_view, info_panel, heatmap};
      (Re-export keeps `crate::palette::X` paths working inside the viewer
       crate without rewriting every use site. Adjust pub-ness if any moved
       symbol was `pub(crate)` — promote to `pub` in gui-core.)

  In `gui/Cargo.toml`:
    Add under [dependencies]:
      sectorforge-gui-core = { path = "../gui-core" }

  Likely fix-ups:
    - `sector_view.rs` imports `crate::editor::...` or similar viewer-only
      modules. If so, those references must be inverted: either (a) move
      the needed symbol into gui-core, or (b) split sector_view into a
      read-only widget (gui-core) + an interactive overlay (gui crate).
      Grep before moving:
        grep -n "^use crate::" gui/src/sector_view.rs gui/src/info_panel.rs \
                                 gui/src/system_view.rs gui/src/heatmap.rs
      Decide per-import: viewer-only goes back; pure-data goes to gui-core.
    - `info_panel.rs` may reference `crate::dashboard` or formatting helpers
      in `gui/src/`. Same triage.
    - `jobs.rs` is tiny (131 lines); should move cleanly.
    - `palette.rs` is data-only (color constants); should move cleanly.

  cargo check --workspace
  [Expected: succeeds. If `crate::palette` paths inside the BUILDER
   (gui/src/builder/) break, that's fine — they'll be relocated in Step 3.4.
   If they break in the viewer, fix the re-export in gui/src/lib.rs.]

  Commit: "split: factor gui-core crate (palette/jobs/widgets)"

Step 3.3  Create the builder crate skeleton
----------------------------------------------------------------------
  mkdir builder builder/src
  Create builder/Cargo.toml:
    [package]
    name = "sectorforge-builder"
    version = "0.1.0"
    edition = "2021"

    [dependencies]
    sectorforge = { package = "sector-generator", path = ".." }
    sectorforge-gui-core = { path = "../gui-core" }
    eframe = { version = "0.29", default-features = false, features = ["default_fonts", "glow", "wayland", "x11"] }
    egui = "0.29"
    rfd = "0.17"
    serde = { version = "1", features = ["derive", "rc"] }
    serde_json = "1"
    toml = "0.8"
    camino = { version = "1", features = ["serde1"] }
    clap = { version = "4", features = ["derive"] }
    thiserror = "1"
    tempfile = "3"
    # Add image / include_dir / rand if the builder code uses them — grep first:
    #   grep -rh "^use \(image\|include_dir\|rand\)" gui/src/builder
    image = { version = "0.25", default-features = false, features = ["png"] }
    include_dir = "0.7.4"
    rand = "0.8"

    [[bin]]
    name = "sectorforge-builder"
    path = "src/main.rs"

  Create builder/src/lib.rs:
    pub mod builder;     # the moved module tree
    pub mod app;         # thin host

    pub use app::BuilderApp;

  Add to root `Cargo.toml` `[workspace] members`: `"builder"`.

Step 3.4  Move the builder module tree
----------------------------------------------------------------------
  git mv gui/src/builder builder/src/builder

  Inside `builder/src/builder/` rewrite the two cross-crate imports:
    builder/src/builder/preview.rs:
      OLD:  use crate::jobs::{spawn_job, JobHandle};
      NEW:  use sectorforge_gui_core::jobs::{spawn_job, JobHandle};

    builder/src/builder/panels/map.rs:
      OLD:  use crate::palette::{ ... };
      NEW:  use sectorforge_gui_core::palette::{ ... };

  Sweep for any remaining `crate::` paths that no longer resolve:
    grep -rn "^use crate::" builder/src/builder --include="*.rs" \
      | grep -v "use crate::builder"
    [Expected after fix: zero matches. If others appear, repoint at
     sectorforge_gui_core::, or move the needed symbol there.]

  In `gui/src/lib.rs`:
    - Delete `pub mod builder;` (line 6).
    - Delete builder-related `pub use` lines if any.

  cargo check -p sectorforge-builder
  [Expected: ERRORS about missing `BuilderApp` / main bin — that's Step 3.5.]

Step 3.5  Author the builder bin entry + thin App host
----------------------------------------------------------------------
  Create builder/src/app.rs (thin eframe::App). Skeleton:

    use std::sync::Arc;
    use eframe::egui;
    use sectorforge::sector_model::GeneratedSector;
    use crate::builder::{BuilderState, BuilderWorkspace};

    pub struct BuilderApp {
        pub workspace: BuilderWorkspace,
        // Add: modal state, file dialog state, status bar string, theme
    }

    impl BuilderApp {
        pub fn new() -> Self { Self { workspace: BuilderWorkspace::default() } }

        pub fn with_initial_state(state: BuilderState) -> Self {
            Self { workspace: BuilderWorkspace::new(state) }
        }
    }

    impl eframe::App for BuilderApp {
        fn update(&mut self, ctx: &egui::Context, _frame: &mut eframe::Frame) {
            // Apply theme — copy from gui/src/app/ui_helpers.rs::apply_theme
            // and either move into gui-core or duplicate here (it's small).
            // Render nav strip (use builder::panels::nav)
            // Render active panel for workspace.active_mut()
            // Render modal layer driven by workspace.active().modal
        }
    }

  IMPORTANT: do NOT try to lift the whole viewer `App` shell. The builder
  bin should be a *thin* host that mounts the existing builder panels via
  the existing `panels::nav` router. Look at:
    builder/src/builder/panels/nav.rs
    builder/src/builder/panels/mod.rs
  and call whatever router function they expose, passing
  `self.workspace.active_mut()`.

  If `nav.rs` calls into helpers that lived in `gui/src/app/*` (theme,
  layout, top-bar), you have three choices, in order of preference:
    (a) Move the helper into gui-core (best when it's pure-egui chrome).
    (b) Duplicate the helper into builder/src/app.rs (best when it's tiny
        and tightly bound to viewer state).
    (c) Lift the helper into a third "shell" crate (only if multiple
        helpers and meaningful reuse — likely overkill).

  Create builder/src/main.rs (model on gui/src/main.rs):

    use std::process::ExitCode;
    use camino::Utf8PathBuf;
    use clap::Parser;
    use sectorforge_builder::BuilderApp;
    use sectorforge_builder::builder::{open_project, new_project, NewProjectOptions};

    #[derive(Debug, Parser)]
    #[command(name = "sectorforge-builder",
              about = "Interactive builder for 40k sectors")]
    struct Cli {
        /// Project directory to open. If omitted, launches with a blank workspace.
        #[arg(long)]
        project: Option<Utf8PathBuf>,
    }

    fn main() -> ExitCode {
        let cli = Cli::parse();
        let app = if let Some(dir) = cli.project {
            match open_project(&dir) {
                Ok(state) => BuilderApp::with_initial_state(state),
                Err(e) => {
                    eprintln!("failed to open project '{dir}': {e}");
                    return ExitCode::from(2);
                }
            }
        } else {
            BuilderApp::new()
        };
        let opts = eframe::NativeOptions {
            viewport: egui::ViewportBuilder::default()
                .with_inner_size([1400.0, 900.0])
                .with_title("sectorforge — builder"),
            ..Default::default()
        };
        match eframe::run_native(
            "sectorforge-builder",
            opts,
            Box::new(move |_cc| Ok(Box::new(app))),
        ) {
            Ok(()) => ExitCode::SUCCESS,
            Err(e) => { eprintln!("gui error: {e}"); ExitCode::from(1) }
        }
    }

  NOTE: `open_project` / `new_project` signatures may differ — read
  builder/src/builder/project_io.rs and adapt. The existing wizard
  (`gui/src/app/...` G6 path) probably required extra args; if the binary
  needs a `NewProjectOptions`, default it.

  cargo check -p sectorforge-builder
  [Expected: succeeds. Fix any remaining import errors module by module.]

  cargo run -p sectorforge-builder --
  [Smoke test: window opens. Don't expect full functionality yet — modal
   wiring may be incomplete. Confirm panels paint without panic.]

  Commit: "split: extract sectorforge-builder bin"

Step 3.6  Slim the viewer crate
----------------------------------------------------------------------
  In `gui/src/lib.rs` confirm:
    - No `pub mod builder;`
    - Re-exports of gui-core modules in place (Step 3.2).

  In viewer `App` (`gui/src/app/mod.rs`), look for any latent builder
  hooks — buttons that opened the builder, menu items, hotkeys. Replace
  the in-process call with an external launch:

    // pseudo:
    use std::process::Command;
    let _ = Command::new("sectorforge-builder")
        .arg("--project").arg(project_dir)
        .spawn();

  If the user wants no launch-from-viewer affordance, just delete the
  button. Ask the user if the affordance should remain — default: keep,
  spawn external process.

  cargo check -p sectorforge-gui
  cargo test -p sectorforge-gui
  [Expected: both succeed.]

  Commit: "split: remove builder from sectorforge-gui"

Step 3.7  Cargo aliases + workspace polish
----------------------------------------------------------------------
  Edit `.cargo/config.toml`:
    [alias]
    sgui     = "run -p sectorforge-gui --"
    sbuild   = "run -p sectorforge-builder --"      # NEW

  Verify root Cargo.toml `members = ["gui", "gui-core", "builder"]`.

  cargo build --workspace
  [Expected: clean. Capture warning count; should be near baseline.]

================================================================================
4. VERIFICATION
================================================================================

After Step 3.7:

  4.1  cargo test --workspace
       [Expected: all tests pass. Builder tests (snapshot, workspace,
        index, command) now live under builder/src/builder/ and are run
        via `cargo test -p sectorforge-builder`.]

  4.2  cargo clippy --workspace -- -D warnings
       [If pre-split was clean, post-split should be clean.]

  4.3  Smoke test viewer:
         cargo run -p sectorforge-gui -- --project <some/project>
       Confirm: loads, renders, no builder code present.

  4.4  Smoke test builder:
         cargo run -p sectorforge-builder -- --project <some/project>
       Confirm: loads, panels paint, can save project.

  4.5  Round-trip:
         a) builder saves to <dir>
         b) viewer auto-reloads same <dir>
       Confirm: viewer FileWatcher (P5) picks up the save. If it doesn't,
       the existing path was builder→builder; verify
       `gui/src/app/lifecycle.rs` (or wherever the viewer's reload lives)
       still watches the project's sector.json. If not, add it — small
       follow-up, not part of the split itself.

  4.6  Verify no duplication:
         grep -rn "fn spawn_job\|fn JobHandle" gui/ builder/ gui-core/ \
            --include="*.rs"
       [Expected: definitions ONLY in gui-core/. Usages elsewhere.]

================================================================================
5. DOC UPDATES (required — CLAUDE.md says: "update GUIDE.md accordingly")
================================================================================

  5.1  CLAUDE.md — update Source layout table:
       - Add `gui-core/src/lib.rs` and key modules.
       - Add `builder/src/main.rs` + `builder/src/app.rs`.
       - Move builder rows out of the `gui/src/` block into a new
         `builder/src/builder/` block.
       Update Commands section:
         cargo build              # builds sectorforge + sectorforge-gui + sectorforge-builder
         cargo run -p sectorforge-builder -- --help

  5.2  AGENTS.md — same Commands update as 5.1.

  5.3  GUIDE.md:
       - Section that currently describes `sectorforge-gui` as
         "viewer/editor for generated sectors": split into two subsections:
         viewer (sectorforge-gui) and builder (sectorforge-builder).
       - The §1452 region needs the builder description moved out.
       - The §2058 file table needs builder rows relocated.

  5.4  OVERVIEW.md — line 21 currently says "two front ends". Change to
       "three front ends" and add the builder paragraph mirroring the
       sectorforge-gui paragraph at line 123.

  5.5  README.md — if it mentions the builder, update similarly.

  5.6  BUILDER_REQS.txt — add a header note:
       "Target crate moved 2026-MM-DD from sectorforge-gui to
        sectorforge-builder. References to gui/src/builder/ in this doc
        now point to builder/src/builder/."
       Do a global search-and-replace of `gui/src/builder/` →
       `builder/src/builder/` inside BUILDER_REQS.txt.

  5.7  GUIBUILDER.txt — same path rewrite.

  5.8  Update MEMORY.md / write a memory:
       Add a [[project]] memory noting the split happened, with date,
       motivation (decoupling before Phase B G6 lands), and the three-crate
       layout. This helps future you avoid wondering "why is the builder a
       separate bin?"

================================================================================
6. GOTCHAS / RISKS
================================================================================

  G1. Hidden cyclic dep: if `gui/src/sector_view.rs` (read widget) imports
      anything from the viewer's editor module, the move to gui-core will
      fail. Triage per Step 3.2 fix-ups. The fallback is to leave
      sector_view in `gui/` and have the builder write its own minimal
      preview widget OR depend on gui (creating a cycle — forbidden).
      Prefer factoring sector_view into a `Read` half (gui-core) and an
      `Edit` overlay (gui).

  G2. `info_panel.rs` is 1,142 lines — the largest moved file. It likely
      hosts a mix of read-only formatters (safe to move) and editor-only
      widgets (must stay in gui or be split). Read it before moving;
      consider splitting into `info_panel/read.rs` (gui-core) +
      `info_panel/edit.rs` (gui).

  G3. `eframe::App` cannot be implemented twice for the same type, but
      each crate has its own `App`/`BuilderApp` types — no conflict. Just
      keep type names distinct to avoid confusion in logs / panics.

  G4. The builder's preview job system (`gui/src/builder/preview.rs`) uses
      `crate::jobs`. Moving jobs to gui-core is required (Step 3.2). If
      `jobs.rs` ever pulls anything from gui internals later, it must be
      pushed back down to gui-core.

  G5. Cargo features: none of these crates currently use feature flags.
      Don't add any during the split — pure structural change, zero
      semantic change, easier review.

  G6. Build time: post-split, `cargo build --workspace` may be marginally
      slower because the workspace has three bins to link. Incremental
      builds of either bin alone will be FASTER. This is the win.

  G7. Release packaging: if there's any CI / installer / release script
      (check `.github/`, `scripts/`, `tools/`), update it to ship both
      `sectorforge-gui` and `sectorforge-builder` binaries. The user has
      not flagged any such script — but check.

  G8. Test that previously lived under `gui/tests/` referencing
      `sectorforge_gui::builder::...` (if any) must move to
      `builder/tests/` and re-path imports. Check:
        grep -rn "sectorforge_gui::builder\|crate::builder" gui/tests builder/tests 2>/dev/null

  G9. The §G6 "open in a new tab" workspace tab strip is already present
      (`gui/src/builder/workspace.rs`). After the split, this lives inside
      the builder bin and works without change. Do NOT try to expose the
      workspace across the bin boundary — single-window per bin is fine.

  G10. If state 1.4 / 1.5 drift checks failed and you proceeded anyway,
       expect more import rewrites in Step 3.4 and possibly the need to
       move additional symbols into gui-core. Budget extra time, don't
       attempt to land the split in one commit.

================================================================================
7. ROLLBACK
================================================================================

Every step in Section 3 ends with a `cargo check` and a commit. To roll
back: `git reset --hard <baseline SHA from Step 1.2>`. Because moves use
`git mv`, history is preserved and the rollback is clean.

If only one step is bad (e.g. Step 3.5 builder bin won't run), revert just
that commit with `git revert <sha>` and re-attempt.

================================================================================
8. OUT OF SCOPE FOR THIS WORK
================================================================================

  - Adding new builder features.
  - Refactoring `BuilderState` internals.
  - Phase B G6 host wiring that's still pending in BUILDER_REQS.txt.
  - Renaming `sectorforge-gui` (keep the user-facing bin name stable).
  - Touching anything in `old/` (CLAUDE.md rule).
  - Re-enabling §14 m42 composition tests (per existing memory:
    [[segmentum-tests-ignored]]).

End of SEPARATE.txt.
