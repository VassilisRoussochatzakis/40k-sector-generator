# BUILDER_REQS.txt
# Requirements: Full GUI Sector Builder (1:1 parity with sectorforge CLI/generator)
# Target crate moved 2026-05-24 from sectorforge-viewer to sectorforge-builder.
# References to viewer/src/builder/ in older revisions now point to
# builder/src/builder/.
# Target app/crate: sectorforge-builder (builder/src/)
# Viewer/editor app/crate: sectorforge-viewer (viewer/src/) -- no builder panels live here.
# Shared egui utility crate: sectorforge-gui-core (gui-core/src/).
# Backing crate: sectorforge (src/)
# Handoff contract: builder writes a project directory; viewer opens the same
# project via `sectorforge-viewer --project <dir>` / `cargo sview --project <dir>`.
# No in-process builder embedding in sectorforge-viewer, no IPC, no shared memory.
# Goal: user constructs an arbitrary sector from scratch, system-by-system,
#       through the dedicated builder GUI alone, with access to every
#       CLI/library feature that affects sector data, derived overlays,
#       validation, or export.

================================================================================
0. SCOPE
================================================================================

The builder must let a user:
  * Start from a blank `GeneratedSector` (no CLI invocation required).
  * Place / move / delete every entity (system, world, route, region, faction,
    presence, claim, control state, orbital asset, surface region, archetype,
    conflict state, intel record, history event, mission, site, persona).
  * Configure every input file (sectorforge.toml, factions.toml,
    relations.toml, route_rules.toml, regions.toml, economy.toml, history.toml,
    system_names.toml, world_names.toml, wishes.toml, map_theme.toml,
    segmentum.toml, worlds.toml).
  * Invoke every derivation pipeline (search, history, personae, hooks,
    relations, regions, economy, prose, missions, sites, briefing,
    interestingness, analyze, diff, compose) without leaving the builder app.
  * Validate (pre + post invariants) and see errors anchored to the
    offending entity in the model.
  * Export every artifact (JSON, Markdown, sector PNG, per-system PNG,
    self-contained HTML, manifest, super-manifest).
  * Re-roll seeds, lock seeds, snapshot/revert via the in-memory history
    layer, all without losing manual edits in unaffected entities.

This document is a checklist. Each item is a separate, testable unit. The
ordering inside a section is roughly dependency-first.

================================================================================
0.1. APP BOUNDARY CONTRACT
================================================================================

B0.   `sectorforge-builder` is the only target app for this checklist. All new
      builder panels, modals, workspace state, command routing, project I/O,
      and builder-specific tests belong under `builder/src/`.

B1.   `sectorforge-viewer` remains the viewer/editor for generated sectors. It may
      keep legacy viewer/editor affordances (sector load, direct sector edit,
      data editor, export UI), but it must not regain `BuilderState`,
      `BuilderWorkspace`, `BuilderCommand`, or `builder/src/builder/panels/*`.

B2.   `sectorforge-gui-core` is the only shared GUI crate. Put reusable,
      app-neutral egui helpers here (jobs, palette, read-only sector/system
      widgets, info formatting, heatmap wrappers). Do not make `gui-core`
      depend on `sectorforge-viewer` or `sectorforge-builder`.

B3.   Builder -> viewer integration is file-based. `sectorforge-builder` saves
      `sectorforge.toml`, referenced catalogs, `<outputs.directory>/sector.json`,
      and manifest data. `sectorforge-viewer --project <dir>` loads/reloads those
      files through its normal project path. Any "open in viewer" affordance
      should spawn `sectorforge-viewer --project <dir>` as an external process.

B4.   Canonical launch commands:
        Builder: `cargo sbuild --project <dir>`
        Builder help: `cargo run -p sectorforge-builder -- --help`
        Viewer/editor: `cargo sview --project <dir>`
      Tests and smoke docs should name the app being launched.

================================================================================
1. RUST ARCHITECTURE PRINCIPLES
================================================================================

R1.   [DONE] Single source of truth = `GeneratedSector` from
      `src/sector_model.rs`. `BuilderState` owns the sole live sector by
      direct ownership behind `&mut BuilderState` — equivalent to the
      `Rc<RefCell<>>` option in the spec (GUI thread is sole writer; jobs
      hold cloned read-only snapshots). Design choice documented in
      `builder/src/builder/state/mod.rs`.

R2.   [DONE] All `BuilderCommand`, `BuilderIndex`, `BuilderState`, and
      `SessionFile` ID fields are the typed `SystemId` / `WorldId` /
      `RouteId` / `FactionId` from `src/ids.rs`. Free strings in these
      modules are only error messages, display names, base64 file payloads,
      or cache keys.

R3.   [DONE] `builder/src/builder/index.rs::BuilderIndex` keys every map with
      `BTreeMap<TypedId, usize>` (or `(usize, usize)` for worlds). Rebuilt
      after every mutation by the command bus.

R4.   [DONE] `BuilderState::run` enforces all four rails:
      (a) invariant re-check via `sectorforge::invariants::check_sector`
      stored in `invariant_report`,
      (b) snapshot/undo stack — redo tail truncated, command pushed,
      `command_cursor` advanced,
      (c) auto-save trigger via `trigger_auto_save` when
      `auto_save_path` is set,
      (d) full `derivation_cache.clear()` (LD2 precision pending).
      Same rails fire on `undo` / `redo`.

R5.   [DONE] BLAKE3 cache helpers: `sectorforge::rng::digest_bytes` and
      `crate::builder::digest_input` (in `derivation_cache.rs`). Cache
      entries are `{ digest, value }` pairs keyed by the digest of the
      canonical-JSON input slice. Reuses the existing `blake3` workspace
      dep — no new crate.

R6.   [DONE] `builder/src/builder/errors.rs::BuilderError` enumerates every
      required variant (`ValidationFailed`, `InvariantViolated`, `IoFailed`,
      `ParseFailed`, `EntityNotFound`, `StaleSnapshot`) plus transparent
      `Mutation` and `Serde` variants for I/O paths.

R7.   [DONE] `gui-core/src/jobs.rs::JobHandle<T>` uses `std::thread::spawn` plus
      `std::sync::mpsc::channel` for results, `Arc<Mutex<f32>>` for
      progress, and `Arc<AtomicBool>` for cancellation. `JobContext::
      set_progress` calls `egui::Context::request_repaint` on each tick.

R8.   [DONE] `builder/src/builder/command.rs::tests::command_log_determinism_blake3`
      replays a fixed `BuilderCommand` sequence twice and asserts the
      `digest_bytes(serde_json::to_vec(&sector))` hex is equal across runs.

R9.   [DONE] Crate-boundary audit after the split:
      `sectorforge-viewer` has no builder dependency and no builder module;
      builder-only dependencies live in `builder/Cargo.toml`;
      shared egui dependencies live in `gui-core/Cargo.toml`;
      cache helpers reuse `sectorforge::rng::digest_bytes`, so neither GUI
      app needs a direct `blake3` dependency for builder cache keys.

R10.  [DONE] Panel convention pinned in `builder/src/builder/panels/mod.rs`:
      every panel is `fn show(ui: &mut egui::Ui, state: &mut BuilderState)`
      — no globals, no `lazy_static`, no raw `String` IDs across the
      boundary, modal state in `BuilderState::modal: Option<ModalKind>`.
      First concrete instance lives in `panels/status.rs` (status bar).

================================================================================
2. CURRENT FRONTEND INVENTORY (what already exists)
================================================================================

Viewer/editor app inventory, reference only (`sectorforge-viewer`; see
`viewer/src/editor/*` and `viewer/src/app/*`). These surfaces are not the builder
host and should not receive new builder requirements:

  [x] New sector dialog (seed + dims), open project, save as
  [x] Hex map: select / drag systems, add system tool, add/delete route tool
  [x] System inspector: name, coord (Q,R), star presence, star colour
  [x] World inspector: type/atmosphere/temp/bio/pop/tech/gov + features
  [x] Routes panel: list + dropdown endpoints + type + stability + distance
  [x] Factions panel: create/delete + assign systems/worlds + designer mode
  [x] Generation panel: seed, dims, system count, world bounds, placement
       mode, cluster bias, world-selection bias, route density
  [x] Settings panel: id/title/seed/dims, route view mode, auto-save
  [x] Wishes panel: edit wishes.toml, run search, apply winning seed
  [x] Data editor: worlds.toml typed inline edit
  [x] Preset gallery: scaffold project/segmentum/sector/system from preset
  [x] Export UI: PNG (sector / all systems / single), HTML, JSON
  [x] Dashboard, history timeline (view), regions (view), relations (view),
       trade (view), route planner (view), segmentum (view)
  [x] Heatmap overlay (control/military/trade/...)
  [x] Subsector overlay rendering

Builder app inventory (`sectorforge-builder`; see `builder/src/main.rs`,
`builder/src/app.rs`, and `builder/src/builder/*`). This is the target surface
for all remaining checklist items:

  [x] Separate binary crate with `--project <dir>` CLI and blank-workspace
      launch when no project is supplied.
  [x] Thin `BuilderApp` host: workspace tabs, builder tab strip, status bar,
      modal layer, watcher drain, live-validation pump, keyboard shortcuts.
  [x] Project lifecycle: new/open/save/save-as, atomic writes, project tree,
      mtime watcher, conflict resolver, recent-projects preferences.
  [x] Generation config G1..G6: full generation config form, seed lock/reroll,
      live preview, apply-with-pinning, partial regen, new-from-preset
      workspace push.
  [x] Builder nav tabs N1..N4: PROJECT, MAP, SYSTEM, WORLD, FACTIONS, CONTROL,
      REGIONS, ROUTES, SUBSECTORS, ECONOMY, RELATIONS, HISTORY, PERSONAE,
      HOOKS, SITES, MISSIONS, PROSE, ANALYTICS, INTERESTINGNESS, SEARCH,
      DIFF, BRIEFING, SEGMENTUM, EXPORT.
  [x] Direct edit Phase B so far: SYSTEM, WORLD, and ROUTES panels plus
      MAP tools; factions/control/claims still governed by the gap matrix below.
  [x] Direct edit Phase C §C1..§C8 + §CL1..§CL4: per-world presence editor,
      add/remove presence, dominance lock, system control_state +
      primary_factions, live PowerProfile preview, and POWER PROJECTION /
      INFLUENCE FIELD MAP overlays driven from the CONTROL tab.
  [x] Direct edit Phase C §E1..§E7 (ECONOMY tab): per-world resource +
      strategic overrides, per-system tithe/supply/priority overrides, stranded
      recompute with red-ring MAP overlay, `economy.toml` editor with Save +
      Apply, lifeline-lane highlight, trade/food/tithe/supply heatmap picker.
  [x] Direct edit Phase C §H1..§H8 (HISTORY tab + WORLD snippet): chronicle
      config grid, eras / event-rule editors, per-event inspector, add-event
      wizard, Regenerate chronicle (preserves `manual = true` entries),
      timeline with focus-on-anchor, and the WORLD inspector
      `§H8 — Chronicle snippets` block.
  [x] Direct edit Phase C §REL1..§REL9 (RELATIONS tab): diplomacy matrix grid
      with click-to-select pair, symmetric + directional cell editor pinning
      attitudes / treaty / 7 numeric dimensions, kind_rules + disposition_rules
      + legacy pair_overrides editors, feed_conflict toggle,
      [generation.relations].min_world_presence slider, Recompute matrix +
      auto-recompute + Save relations.toml passthrough.
  [x] Direct edit Phase D §PER1..§PER5 (PERSONAE tab): per-faction-kind pool
      editor (names/titles/traits) with custom-kind add + reset, per-anchor
      derived roster with cross-tab anchor links, `[[manual]]` editor (manual
      personae survive regenerate), `min_world_dominance` + per-anchor caps,
      Auto-derive + auto-recompute toggle, agenda derivation source tooltip,
      Save personae.toml passthrough.
  [x] Validation/invariant panels and health status pip.
  [x] Shared jobs/palette/widgets pulled from `sectorforge-gui-core`.

================================================================================
3. GAP MATRIX (must-build for parity)
================================================================================

Legend: [G] = gap, [P] = partial, [X] = exists. Section refs map to §6+.
Statuses refer to `sectorforge-builder` unless a row explicitly says
"viewer-only".

  Domain              Status    Section
  ------------------- --------  ----------
  Generation config   [X]       §6
  System edit         [P]       §7
  World edit          [P]       §8
  Route edit          [X]       §9
  Faction edit        [P]       §10
  Control / presence  [X]       §11
  Claims              [G]       §12
  Subsectors          [P]       §13   (view only; need reassignment)
  Regions             [G]       §14   (view only; need place/grow/edit)
  Economy             [X]       §15   (per-world / per-system overrides, lifelines, heatmaps)
  Relations           [X]       §16   (matrix + overrides + kind/disposition rules + feed_conflict + min_world_presence + recompute + save)
  History             [X]       §17   (chronicle config, eras, rules, event editor, add wizard, regenerate, timeline, world snippets)
  Personae            [X]       §18   (kind pool editor, manual entries, dominance tier + caps, auto-derive + auto-recompute, agenda tooltip)
  Hooks               [G]       §19
  Sites               [G]       §20
  Missions            [G]       §21
  Prose / gazetteer   [G]       §22
  Briefing            [G]       §23
  Interestingness     [X]       §24
  Analytics           [P]       §25   (dashboard exists; need full §8 fields)
  Search              [P]       §26   (wishes UI exists; need full constraints)
  Diff + ticks        [G]       §27
  Conflict + stability[X]       §28
  Intel / visibility  [X]       §29   (per-system + per-world editors, baseline derive_intel, observer lens, player cutoff)
  Archetypes          [X]       §30   (per-system AR1 editor + AR2 sector-wide apply_all + AR3 per-axis enable mask)
  Orbital assets      [G]       §31
  Surface regions     [G]       §32
  Validation surface  [P]       §33
  Invariants surface  [P]       §33
  Export bundle       [P]       §34   (need manifest write, super-manifest)
  Themes / heatmaps   [P]       §35
  Segmentum compose   [G]       §36   (view only; need build pipeline)
  Project / files     [P]       §37
  Undo / snapshots    [G]       §38
  Live derivations    [G]       §39

================================================================================
4. DATA-MODEL TASKS (foundation)
================================================================================

D1.   [DONE] `GeneratedSector::empty(id, title, seed, width, height)` constructor
      in `src/sector_model.rs`. Returns valid empty sector that passes
      pre-validation. Required because the builder starts blank.

D2.   [DONE] `GeneratedSystem::new_at(id, coord, name)` and `GeneratedWorld::new(
      id, name)` constructors. Default enum values must be the
      lowest-impact variants (e.g. `Atmosphere::Standard`,
      `TechLevel::Industrial`). Document defaults in doc-comments.

D3.   [DONE] Mutation API on `GeneratedSector` (new module
      `src/sector_model/mutation.rs`):
        - `add_system`, `remove_system`, `move_system`
        - `add_world_to_system`, `remove_world`
        - `add_route`, `remove_route`, `swap_route_endpoints`
        - `add_faction`, `remove_faction`
        - `add_world_presence`, `remove_world_presence`
        - `add_claim`, `remove_claim`
        - `set_system_control_state`, `set_world_dominance`
        - `add_region`, `remove_region`, `add_region_hex`,
          `remove_region_hex`
        - `add_orbital_asset`, `remove_orbital_asset`
        - `add_surface_region`, `remove_surface_region`
        - `set_archetype`, `clear_archetype`
        - `add_intel_record`, `remove_intel_record`
        - `add_history_event`, `remove_history_event`, `edit_event`
        - `set_economy_override`, `clear_economy_override`
        - `set_relation_override`, `clear_relation_override`
      Each method returns `Result<(), MutationError>`. Each method is the
      single canonical entry — no panel mutates fields directly.

D4.   [DONE] Re-index pass (`GeneratedSector::reindex_ids`) manages `sys-NNNN`,
      `sys-NNNN-wNN`, `route-sys-A-sys-B` after structural changes.
      Supports stable IDs across renames using a tombstone table
      (`id_history` in `GeneratedSector`) when `stable_ids_on_rename`
      is enabled in the builder.

D5.   [DONE] `BuilderState` (in `builder/src/builder/state/mod.rs`; method
      impl blocks split per concern across `state/{types, selection, undo,
      derivations, regions_ops, generation_ops}.rs`):
        - `sector: GeneratedSector`
        - `project_path: Option<Utf8PathBuf>` (use `camino`)
        - `config: SectorforgeConfig` (from `src/config.rs`)
        - `data_catalogs: DataCatalogs` (world rows, names, faction rows,
          relation tables, route rules, region defs, economy defs)
        - `index: BuilderIndex`
        - `command_log: Vec<BuilderCommand>` (for undo + replay)
        - `snapshots: Vec<Snapshot>` (named save-points)
        - `pinned_systems: BTreeSet<SystemId>` (side-table for Q1)
        - `pinned_worlds: BTreeSet<WorldId>` (side-table for Q1)
        - `derivation_cache: DerivationCache`
        - `dirty: bool`, `auto_save_path: Option<Utf8PathBuf>`
        - `validation_report: Option<ValidationReport>`
        - `invariant_report: Option<InvariantReport>`
        - `modal: Option<ModalKind>`
        - `pending_jobs: Vec<JobHandle>` (for off-thread work)

D6.   [DONE] Implement `Serialize/Deserialize` for `BuilderState` so the in-progress
      builder session can be saved as `.sgforge`. Use a JSON envelope
      containing base64-encoded files by default to avoid new dependencies.
      Optionally support a `.tar` format toggle for portability if the `tar`
      crate is enabled. Use `serde_json` only; no bincode.

================================================================================
5. PROJECT + I/O TASKS
================================================================================

P1.   [DONE] New project wizard: blank project at `<dir>/`, writes default
      `sectorforge.toml`, empty `worlds.toml`, optional preset overlay.
      `presets::scaffold_to_dir(preset_id, dst, seed_override)` helper added
      in `src/presets.rs`. Wizard panel in
      `builder/src/builder/panels/new_project.rs` drives
      `project_io::new_project` which scaffolds the directory and loads it
      back via the §P2 path so the in-memory state is identical to a fresh
      open.

P2.   [DONE] Open project: parse `sectorforge.toml` via
      `sectorforge::input::load_project`, load all referenced files (worlds,
      names, factions, route_rules, relations, regions, economy, history)
      into `BuilderState::data_catalogs`. Sector loaded from
      `<outputs.directory>/sector.json` when present, else blank empty
      sector matching dimensions. Parse errors surface as
      `BuilderError::ParseFailed { file, message }` — `toml` crate's
      messages already carry `line N, column M` info. Picker panel in
      `builder/src/builder/panels/open_project.rs`.

P3.   [DONE] Save project: writes sectorforge.toml + every present catalog
      back under the project root, then `<outputs>/sector.json` and (when
      `write_manifest` is true) `<outputs>/manifest.json`. Each write is
      atomic via tmp + rename (R9 forbids new crates so we don't use
      `tempfile::NamedTempFile::persist`; the strategy is the same).
      Manifest input_digests are updated in-place to mirror the files we
      just wrote. Action panel in
      `builder/src/builder/panels/save_project.rs` exposes Save and Save-as
      buttons.

P4.   [DONE] Project tree view (new tab `PROJECT`).
      `builder/src/builder/panels/project_tree.rs` renders a collapsible
      directory tree rooted at `BuilderState::project_path`. Folders
      sort-first then alpha; hidden (`.<name>`) entries skipped. Files
      already marked in `BuilderState::dirty_files` render with a leading
      "● " in yellow. Click sets `BuilderState::selected_file` —
      the §PF2 TOML editor tabs (Phase E) will read it to decide which
      buffer to open.

P5.   [DONE] File watcher.
      `builder/src/builder/file_watcher.rs` spawns an mtime-polling thread
      (R9 forbids new crates so we don't take the suggested `notify` dep).
      The watcher samples every file the loader hashed at open-time,
      ticks at 1 Hz, and posts `FileChange { rel_path, mtime }` via
      `mpsc`. The main UI loop drains via
      `project_io::drain_watcher_events`: when the in-memory buffer is
      clean it silently re-parses the affected catalog via
      `project_io::reload_catalog`; when dirty (global `state.dirty` or
      the file is in `dirty_files`) it raises
      `ModalKind::ConflictResolver { rel_path }` and the
      `panels/conflict_resolver.rs` dialog offers Reload-from-disk or
      Keep-in-memory.

P6.   [DONE] Recent projects list in Preferences.
      `builder/src/builder/preferences.rs::Preferences` (`recent_projects:
      Vec<Utf8PathBuf>`) round-trips via
      `~/.config/sectorforge/preferences.toml`. Loaders tolerate missing
      or malformed files. `push_recent` dedupes + bumps to front,
      truncates to 10. Wired into `open_project` and `save_project_as` so
      every successful project hit lands at the top of the MRU. UI lives
      in `panels/preferences.rs` — click an entry to open it.

================================================================================
6. GENERATION CONFIG
================================================================================

G1.   [DONE] Generation panel reaches feature parity with `[generation]` and
      `[generation.placement]` / `[generation.world_selection]` /
      `[generation.routes]` / `[generation.relations]` tables in
      `sectorforge.toml`. Fields:
        seed, sector_width, sector_height, subsector_width,
        subsector_height, system_count, min/max worlds per system,
        allow_empty_hexes, world_feature_count, strict_world_rows,
        placement.mode (uniform_grid|weighted_grid|clustered),
        placement.cluster_bias, placement.minimum_system_distance,
        world_selection.mode, world_selection.require_complete_rows,
        world_selection.allow_partial_rows,
        world_selection.same_star_colour_bias,
        world_selection.strict_same_star_colour,
        world_selection.avoid_duplicate_world_type_in_system,
        routes.enabled, routes.max_route_distance, routes.route_density,
        routes.ensure_connected_graph,
        relations.min_world_presence.
      Implementation: `builder/src/builder/panels/generation.rs::show_g1_parameters`
      binds every listed field to a typed widget (ComboBox over enums,
      DragValue for ints, Slider for fractions) and mutates
      `state.config.generation` directly. The panel is hosted from the
      PROJECT tab as a `Generation (§6)` collapsing header so the §N1
      24-tab strip stays stable.

G2.   [DONE] Seed-lock toggle. When locked, "Re-roll" preserves
      `[generation].seed`; when unlocked, increments via
      `blake3("sectorforge:{seed}:reroll:{n}")`.
      Implementation: `BuilderState::{seed_locked, seed_reroll_counter}`
      plus `BuilderState::reroll_seed` (returns the new seed and rewrites
      `config.generation.seed`). Counter monotonically advances on every
      unlocked re-roll; the digest helper lives at
      `builder/src/builder/preview.rs::derive_reroll_seed`. Tests
      `reroll_locked_keeps_seed`,
      `reroll_unlocked_advances_seed_and_counter`, and
      `derive_reroll_seed_is_deterministic_and_counter_sensitive` pin the
      contract.

G3.   [DONE] Live preview mode: every config edit triggers a debounced
      regeneration (200 ms). Run on worker thread; partial preview into
      a scratch `GeneratedSector` displayed under a "PREVIEW" badge.
      Implementation: `builder/src/builder/preview.rs::PreviewState` owns the
      debounce timer, scratch sector, in-flight `JobHandle`, and a
      revision counter that discards stale results. `PreviewState::pump`
      is called per-frame from the panel; the worker runs
      `sectorforge::generation::generate_with_progress_and_cancel`
      through `sectorforge_gui_core::jobs::spawn_job`. Default debounce =
      `DEFAULT_DEBOUNCE_MS` = 200 ms. The panel surfaces the scratch
      sector counts under a coloured "PREVIEW READY" line.

G4.   [DONE] "Apply" promotes preview to active sector and overwrites
      `BuilderState::sector`. Preserves manual edits to systems flagged
      pinned (resolved per Q1 to a `BuilderState::pinned_systems`
      side-table, not a `GeneratedSystem` field — see Q1 / D5 / S3).
      Implementation: `BuilderState::apply_preview` swaps the scratch
      sector in, then for each `SystemId` in `pinned_systems` restores
      the pre-preview entry at the same id (or re-inserts it if the
      preview dropped the slot). It then rebuilds the index, clears the
      derivation cache, marks the project dirty, and re-runs invariants.

G5.   [DONE] Per-subsector partial regeneration: select a rectangle / cluster of
      hexes, regenerate only those systems. Reuses
      `sectorforge::generate_system_standalone` per slot. Pinned systems
      are skipped. `BuilderState::regions` are exposed via the in-memory
      catalogs the partial regen synthesises into a `ProjectInput`, so
      warp regions affect downstream draws.
      Implementation: `BuilderState::{partial_regen_rect,
      regenerate_partial}` plus `PartialRegenRect::{from_corners,
      contains}`. The panel exposes min/max q/r DragValues; on apply,
      every non-pinned in-rect system is replaced by a fresh
      `generate_system_standalone` call keyed by its existing index.

G6.   [DONE] "New sector from preset" inside the builder — reuses §P1
      logic but keeps the current builder open in a new tab via a new
      `BuilderWorkspace` (`builder/src/builder/workspace.rs`). The
      workspace owns `Vec<BuilderState>` plus an active index;
      `push`/`switch_to`/`close_active` drive the tab strip. The panel
      collects (preset id, destination, seed), calls
      `project_io::new_project` with `preset = Some(id)`, and pushes the
      resulting state into the `BuilderApp` workspace. Library tests and
      non-app callers may still use the fallback path that replaces the
      current state, but the shipping `sectorforge-builder` app always
      provides a workspace. Modal lifecycle uses `ModalKind::NewFromPreset`.

================================================================================
7. SYSTEMS
================================================================================

S1.   [DONE] Toolbox: ADD SYSTEM (click hex), DELETE SYSTEM (click system),
      MOVE SYSTEM (drag), RENAME (double-click label). Implemented in
      `builder/src/builder/panels/map.rs` over the §N3 `MapTool` arming +
      `BuilderState::pending_place` / `pending_rename`. Geometry helpers
      mirror `editor/map_panel.rs` but talk to `BuilderState`.

S2.   [DONE] System inspector covers every field of `GeneratedSystem`:
      id (read-only, rename via D4 toggle), name, coord (with validity
      check), star (presence, colour, optional secondary star),
      worlds (managed in §8), routes (read-only edge list),
      primary_factions (managed in §10), control (managed in §11),
      tags (free-text + canonical-tag picker), notes (multi-line),
      orbital_assets (§31), blockade (§31), conflict (§28),
      intel (§29), archetype (§30). See `builder/src/builder/panels/system.rs`.

S3.   [DONE] `pinned` field (D5) toggleable from the inspector. Prevents the
      generator from touching the system during re-roll / partial regen.
      Stored in `BuilderState::pinned_systems` (side-table). The MAP
      panel paints a coral ring around pinned systems; the §S5 regen and
      the §S4 reseed both skip pinned entries.

S4.   [DONE] Bulk operations: select multiple systems (rect / shift-click) →
      rename pattern, reseed worlds, reassign primary faction, flip
      control state, pin/unpin. Selection lives in
      `BuilderState::selected_systems`; ops live under the §S4 collapse
      in the SYSTEM panel.

S5.   [DONE] "Generate one system here" via `generate_system_standalone`. Useful
      for filling a single hex without rerunning the full generator.
      Honour `--coord-q/r/--index/--seed` as inline controls. The
      command bus carries `BuilderCommand::ReplaceSystem` so undo
      restores the prior occupant.

S6.   [DONE] Coord validity: reject coords outside `(0..sector_width,
      0..sector_height)`. Coord collisions resolved with a swap or
      cancel dialog. The dialog (`PendingCollision`) fires from drag-drop
      and from the inspector "Apply coord" button; the Swap path runs
      `BuilderCommand::SwapSystems`, backed by
      `GeneratedSector::swap_systems` in `src/sector_model/mutation.rs`.

================================================================================
8. WORLDS
================================================================================

W1.   [DONE] World inspector in `builder/src/builder/panels/world.rs` covers
      every `GeneratedWorld` field: id, index, source_row_index, name,
      orbit, star_colour, world_type, atmosphere, temperature, biosphere,
      population, tech_level, government, notable_features, tags, notes,
      faction presence, claims (§12), control (§11), surface_regions
      (§32), conflict (§28), stability (§28).

W2.   [DONE] Enum pickers driven by `*::VARIANTS` directly from
      `src/worlds.rs`, with `display_name()` for labels. Eliminates
      drift from the legacy `viewer/src/editor/enums.rs` string arrays —
      every variant added to the enum appears in the picker
      automatically. Covered by
      `enum_picker_variants_match_worlds_authoritative_set`.

W3.   [DONE] `pinned` toggle on world. Stored in
      `BuilderState::pinned_worlds` (side-table). Toggle lives in the
      Identity collapsing header.

W4.   [DONE] "Re-roll this world" button. Calls
      `BuilderState::regenerate_world` which builds the pool from
      `data_catalogs.worlds`, then dispatches to the new
      `sectorforge::generation::regenerate_world_payload` helper.
      Pinned worlds refuse. Each click bumps
      `BuilderState::world_reroll_counter` so the per-world stream
      yields a distinct draw deterministically.

W5.   [DONE] Notable features picker. Searchable multi-select with
      weight preview blended from per-world-type, per-star-colour, and
      global tiers of the in-memory pool. Already-present features are
      hidden from the picker.

W6.   [DONE] Coupling warnings rendered inline as a collapsing header
      (yellow, non-blocking). Heuristics include DeathWorld + High-Tech,
      DeadWorld with population, TombWorld + Thriving biosphere,
      Uninhabited with non-None government, Airless + Thriving
      biosphere, ForgeWorld + low tech, etc.

W7.   [DONE] Claims chip-row directly in the world inspector. Each
      chip is colour-coded per `ClaimType`, deep-links to the FACTIONS
      tab, and has a × button. Add-claim row picks faction + claim
      type + strength below the chip strip.

================================================================================
9. ROUTES
================================================================================

R1.   [DONE] Route inspector in `builder/src/builder/panels/routes.rs`
      covers every `GeneratedRoute` field: id, endpoints, type
      (StableWarpLane | ChartedPassage | SecretPassage | Webway |
      BlackShip | SmugglingLane), stability, distance with auto-distance
      display, tags, and editable per-faction `RouteControl` rows.

R2.   [DONE] Add-route tool is wired in `builder/src/builder/panels/map.rs`:
      click-click or drag from first system to second system runs
      `BuilderCommand::AddRoute`; distance is auto-computed from
      `hex_distance`.

R3.   [DONE] Manual distance override lives in the route inspector. It shows
      auto distance, a "Use auto" action, and the warning
      `ROUTE_DISTANCE_MISMATCH will fire unless distance == hex_distance`.

R4.   [DONE] Bulk operations support predicates over route type, stability,
      tag substring, and region-crossing hex-line; actions set matching
      route type or stability (e.g. all Charted Passages crossing region X
      -> Hazardous).

R5.   [DONE] Route-rules editor (`data/routes/route_rules.toml`) edits
      default_weight, max_distance, preference flags, and modifier rows for
      notable_feature, world_type, government, and route_type. Changes mark
      the route_rules file dirty and schedule the live preview. Core
      `RouteCondition` now includes `route_type`, and generation applies
      government + route_type modifiers.

R6.   [DONE] Hidden routes panel uses `src/hidden_routes.rs::HiddenRoutesConfig`
      to set kind (Webway/BlackShip/SmugglingLane), explicit endpoints,
      K-nearest count, and Blackout-region exclusion, then emits deduplicated
      hidden-route edges.

R7.   [DONE] Routes "ensure connected" toggle re-runs the MST-style bridge
      connector after route edits/removals and via a manual "Run connector
      now" action, adding `bridge` routes until the route graph is one
      component.

================================================================================
10. FACTIONS
================================================================================

F1.   [DONE] Faction editor reaches `Faction` (`src/factions.rs`) parity:
      id, name, kind (Imperial | Mechanicus | Ecclesiarchy | ...),
      disposition (Lawful | Pragmatic | Zealous | Corrupt), weight,
      preferred_world_types, preferred_governments,
      preferred_notable_features, FactionStyle (fill/accent/border/glyph).
      Implemented in `builder/src/builder/panels/factions.rs`: identity grid,
      kind/disposition combos seeded from the known data/factions/factions.toml
      vocabulary, weight `DragValue`, and searchable multi-pickers backed by
      `WorldType::VARIANTS`, `Government::VARIANTS`, and
      `NotableFeature::VARIANTS`.

F2.   [DONE] FactionStyle override panel: colour pickers, glyph picker, preview
      tile in legend. `FactionDef` gains optional `style_fill`,
      `style_accent`, `style_glyph`, `style_border`; new
      `faction_style::faction_style_rgb_with_overrides` applies them on top
      of the kind/id derivation so the bitmap/SVG/HTML renderers can opt in.

F3.   [DONE] Subfaction + force editor: hierarchical list (faction > subfaction >
      force). Each force row maps to a TOML row in `factions.toml`. The
      roster pane groups rows by `FactionDef::top_faction_id()` and
      `subfaction_id()`; the inspector exposes the optional
      `faction`/`faction_name`/`subfaction`/`subfaction_name` fields so a row
      can move between hierarchy buckets.

F4.   [DONE] "Recompute style from kind" button regenerates style via
      `faction_style.rs` defaults by clearing every per-row style override.

F5.   [DONE] World/system presence assignment via §11. Inspector renders the
      current `sector.factions[i]` presence counts and a deep-link button
      that focuses the row and switches to `BuilderTab::Control` so the
      Phase C §11 presence editor picks it up.

F6.   [DONE] Export: write modified roster back to
      `data/factions/factions.toml`. "Save factions.toml" calls
      `project_io::save_project` after auto-pointing
      `config.inputs.factions` at the default relative path when it is
      unset.

F7.   [DONE] Importance / visibility: per-faction toggle for legend display
      (uses `src/importance.rs`). `FactionDef.legend_visible` is a
      tri-state (`None` = auto via `compute_display_buckets`,
      `Some(true)` = force visible, `Some(false)` = force hidden) and the
      roster pane dims hidden rows.

================================================================================
11. CONTROL + PRESENCE
================================================================================

C1.   [DONE] Per-world presence editor in
      `builder/src/builder/panels/control.rs::show_world_presence_editor`. The
      panel reads `BuilderState::selected_world_id` and renders an
      `egui::Frame::group` per `WorldFactionPresence` with: influence-tier
      ComboBox (Dominant | Significant | Minor | Hidden), `intel_confidence`
      DragValue 0..=100, and a 10-row dimension grid (admin / military /
      orbital / economic / industrial / ideological / covert / logistics /
      legitimacy / visibility) of `egui::Slider` 0..=100 with
      `SliderClamping::Always`. Edits are diffed via the local
      `presence_changed` helper so only real moves arm validation.

C2.   [DONE] `show_add_presence_row` filters the sector roster against the
      world's existing presence rows so the same faction cannot be
      double-added. The "+ presence" button pushes a fresh
      `WorldFactionPresence` with default dimensions, the chosen tier
      (Dominant..Hidden ComboBox), `intel_confidence = 100`, and
      `relationship_to_government = "neutral"`. Every §C1 row carries an
      inline "× remove" button that also clears any
      `BuilderState::dominance_locked` marker scoped to that pair.

C3.   [DONE] Dominance ComboBox per (world, faction) over the six
      `DominanceState` variants. Side-table `BuilderState::dominance_locked:
      BTreeSet<(WorldId, FactionId)>` records the manual-lock decision;
      unlocked rows are continuously refreshed from
      `DominanceState::from_score(local_control_score)` (the §C3 derivation —
      `src/control.rs::derive_world_control` is also invoked below for a
      per-world echo). Picking a state auto-arms the lock; the "manual lock"
      checkbox releases it.

C4.   [DONE] `show_system_control_editor` renders a `control_state` ComboBox
      keyed by `BuilderState::selected_system_id`. Options cover every
      `SystemState` variant (Pacified | Fragmented | Blockaded | Warzone |
      Infiltrated | Quarantined | Uncharted) plus an explicit `(none)` entry
      that clears the override. Edits write through
      `state.sector.systems[i].control.state` and arm validation.

C5.   [DONE] Same panel section drives `primary_factions`. The top-3 entries
      are recomputed every frame from
      `sectorforge::control::derive_system_control(...).top_factions`. A
      "manual override" toggle records the system id in
      `BuilderState::primary_factions_locked` so subsequent renders leave the
      list alone; a "Recompute" button restores the auto-derived top-3 and
      clears the lock in one click.

C6.   [DONE] `show_power_profile_preview` calls
      `sectorforge::control::aggregate_faction_power` per frame and renders a
      10-column egui Grid (faction, admin, mil, naval, econ, ind, ideo,
      covert, logi, legit, total). Values are colour-coded by magnitude via
      `power_color`. An "Apply to sector rollups" button forwards the same
      aggregate through `control::apply_faction_power` so the
      `GeneratedFaction.power` field used by exports stays in sync.

C7.   [DONE] CONTROL panel exposes a three-way overlay toggle
      (OFF / POWER PROJECTION / INFLUENCE FIELD) backed by
      `BuilderState::control_overlay: ControlOverlay`. POWER PROJECTION calls
      `panels::control::build_overlay_cells`, which runs
      `sectorforge::power_projection::project_sector`, normalises the
      per-system reach against the sector max, and emits
      `HashMap<SystemId, HeatCell>` keyed by system id (colour from
      `faction_style_by_id`). The MAP panel forwards the map through
      `SectorView::heatmap` so the falloff renders without extra plumbing.
      Covered by `build_overlay_power_projection_keys_systems_with_power`.

C8.   [DONE] Same overlay toggle selects INFLUENCE FIELD; the helper runs
      `sectorforge::influence_field::build` and samples the `dominant` cell
      at every system coord, scaling cell `score` into `HeatCell::intensity`.
      Empty sectors yield an empty map (verified by
      `build_overlay_influence_field_handles_empty_sector`). Both overlays
      clear when the toggle is set back to OFF.

================================================================================
12. CLAIMS
================================================================================

CL1.  [DONE] Per-world claim editor: chip-row of (faction_id, claim_kind).
      Claim kinds (from `src/control.rs`): Legal, Imperial, Religious,
      Dynastic, Commercial, Military, Ancient, Hunting, Covert,
      Rebellion, Treaty. Implemented in `builder/src/builder/panels/control.rs`
      as a scrollable per-world list with strength badge and per-chip remove
      button; deep-links to the WORLD inspector via `→ WORLD`. The same
      chip-row is also rendered on the WORLD tab (§W7).

CL2.  [DONE] "Add claim" picker. Multiple claims per faction allowed (e.g.,
      Legal + Military by the same dynasty). Picker mirrors §W7: faction
      ComboBox + ClaimType ComboBox + strength DragValue<u8> 0..=100,
      append-only — duplicate (faction, kind) pairs are permitted by design.

CL3.  [DONE] "Contested" auto-flag computed from N>1 distinct claimants.
      Surfaced as a per-world badge on the CONTROL panel and as an
      aggregate collapsing header (`§CL3 — Contested (n)`) at the top of
      the tab, with deep-link buttons to jump to the WORLD inspector. A
      "contested only" checkbox filters the per-world list.

CL4.  [DONE] Bulk: convert all claims of kind X by faction Y to kind Z.
      Implemented in the `§CL4 — Bulk convert claims` section of the
      CONTROL panel: faction Y + claim X + target Z dropdowns with a live
      "matches: N" counter; the apply button is disabled when X = Z or
      the match count is zero. Walks every world in the sector in one
      pass and arms `state.dirty` + validation debounce on apply.

================================================================================
13. SUBSECTORS
================================================================================

SUB1. [DONE] Subsector panel: list of clusters with label (A, B, ...), capital,
      contained systems, dominant faction. Sortable striped grid in
      `builder/src/builder/panels/subsectors.rs::show_cluster_list`, fed by
      `current_subsectors` (reuses `BuilderState::map_view_cache` when warm).

SUB2. [DONE] "Recluster" button runs `src/subsectors/mod.rs` k-means with
      configurable `target_systems_per_subsector`. Live DragValue +
      explicit "Recluster" / "Reset target" buttons mutate
      `BuilderState::subsector_target_systems`, which feeds the
      `MapViewCache` digest in `panels::map::sector_view_digest` so the
      cluster cache rebuilds on the next MAP tick.

SUB3. [DONE] Manual reassignment via per-system "Move to..." picker (drag-drop
      tree view replaced with ComboBox; same semantic, fewer pixels).
      Each move writes `BuilderState::subsector_system_overrides` and flags
      both source and destination clusters in `subsector_manual`.
      `apply_subsector_overrides` reapplies the overrides on top of every
      fresh k-means pass so manual splits survive SUB2 reclustering.

SUB4. [DONE] Capital override per subsector. ComboBox over the cluster's
      current `system_ids` writes `subsector_capital_overrides` (keyed by
      subsector id, value = chosen `SystemId`). Applied post-cluster in
      `apply_subsector_overrides`; ignored when the chosen system is not a
      member of that cluster (guards against stale entries after a SUB3
      reassignment).

SUB5. [DONE] Subsector colour override (FactionStyle-derived default).
      `egui::Ui::color_edit_button_srgb` swatch backed by
      `subsector_colour_overrides`; default colour comes from
      `sectorforge::faction_style::faction_style_rgb_by_id` on the cluster's
      `summary.controlling_faction_id` (grey 110/110/120 when no controlling
      faction).

================================================================================
14. REGIONS (WARP PHENOMENA)
================================================================================

REG1. Region table editor: id, name, kind (WarpStorm | Turbulence |
      CalmCorridor | Blackout | Anomaly), hex list, intensity, notes.

REG2. Region painting tool: brush hexes onto the map (left-click paint,
      right-click erase). Constraint: no overlap with existing regions
      (invariant `REGION_HEX_OVERLAP`).

REG3. "Grow seeded region" command: pick seed hex + mean_size + kind →
      runs `src/regions.rs::grow_blob` deterministically.

REG4. Live effect preview: route stability changes show on the map as
      the user edits regions (e.g., WarpStorm → routes crossing the
      footprint go red).

REG5. Region config editor (`data/routes/regions.toml`): enabled flag,
      count, mean_size, seed, per-kind weights.

REG6. Validation: `REGION_HEX_OUT_OF_BOUNDS`, `REGION_HEX_OVERLAP`,
      `REGION_ISOLATES_SECTOR` surfaced inline.

REG7. Region glyph preview (`~ ^ = # *`) in markdown preview tab.

================================================================================
15. ECONOMY
================================================================================

E1.   [DONE] Per-world economy override: 6-tuple (ore, promethium,
      foodstuffs, manufactured, archeotech, recruits) signed sliders. ECONOMY
      panel `§E1` renders an `egui::Slider` -100..=100 per axis bound to
      `BuilderState::world_economy_overrides: BTreeMap<WorldId, ResourceVector>`.
      `BuilderState::recompute_economy` runs `economy::derive_with` with the
      live `data_catalogs.economy` (forced `enabled = true`), pins each
      overridden world's `WorldEconomy.vector`, then recomputes per-system
      surplus / shortage aggregates from the patched rows so the override
      propagates without re-running the full graph. "Clear override" drops
      the entry and the next recompute restores the derived value.
      Covered by `world_override_pins_vector_through_recompute`.

E2.   [DONE] Strategic output editor: per-world `StrategicOutput` (food, ore,
      manufacturing, arms, ships, pilgrimage, psyker_tithe, manpower,
      knowledge, xenos_value) with a slider per axis (0..=100) bound to
      `BuilderState::world_strategic_overrides: BTreeMap<WorldId,
      StrategicOutput>`. `recompute_economy` pins the override into
      `WorldEconomy.strategic_output` and re-aggregates the system-level
      strategic vector + sector totals from the patched worlds.
      Covered by `strategic_override_replaces_derived_output`.

E3.   [DONE] Per-system tithe_status / supply_risk / strategic_priority
      override row. ECONOMY panel `§E3` lists every `SystemEconomy` with
      a ComboBox per axis bound to
      `BuilderState::{system_tithe_overrides, system_supply_overrides,
      system_priority_overrides}` (all `BTreeMap<SystemId, ...>` side-tables).
      Overrides are pinned after the auto-derived pass; the "× clear" button
      drops all three for that system. A "→ SYSTEM" deep-link focuses the
      row in the SYSTEM tab.
      Covered by `system_overrides_pin_after_recompute`.

E4.   [DONE] Stranded recompute on every edit + map indicator.
      `recompute_economy` runs after every E1..E3 mutation and the panel
      auto-applies it. The §E4 collapsing list enumerates every stranded
      world (`WorldEconomy.stranded`) with a deep-link to the WORLD
      inspector. The MAP panel paints a red ring around every system whose
      id is in `panels::economy::stranded_system_ids(state)` via a post-pass
      `ui.painter().circle_stroke` overlay layered on top of
      `SectorView::show`.

E5.   [DONE] `economy.toml` editor on the ECONOMY tab. Renders
      `enabled` / `feed_stability` toggles plus collapsing editors for
      `by_world_type` (one `ResourceVector` per row), `by_tech_level`, and
      `by_population` (f32 multipliers). Live edits arm
      `state.dirty_files`; "Save economy.toml" round-trips through
      `project_io::save_project` (auto-pointing `config.inputs.economy`
      at `data/worlds/economy.toml` when unset). "Apply & recompute"
      flushes the edits through `recompute_economy` immediately.

E6.   [DONE] Lifeline-lane visualiser. ECONOMY panel `§E6` exposes
      `BuilderState::economy_highlight_lifelines: bool` plus
      `economy_lifeline_min_score: f32` (default 35.0), and lists the
      top dependency edges above the threshold (sorted by score). The MAP
      panel feeds `panels::economy::lifeline_route_ids(state)` into
      `SectorView::path_route_ids`, reusing the existing thick-glow route
      highlight already used by the path planner.

E7.   [DONE] Trade-volume heatmap mode picker.
      `BuilderState::map_heatmap_mode: HeatmapMode` (default `Off`) is a
      ComboBox over `Off | TradeVolume | FoodOutput | TitheStress |
      SupplyVulnerability`. The MAP panel calls
      `sectorforge_gui_core::heatmap::compute(&sector, mode)` when the
      §C7 / §C8 control overlay is `None` and the mode is not `Off`,
      then feeds the resulting `HashMap<SystemId, HeatCell>` into
      `SectorView::heatmap`. Control overlays still win when active so
      the trader heatmap and the influence overlay never compete.

================================================================================
16. RELATIONS (DIPLOMACY)
================================================================================

REL1. [DONE] Diplomacy matrix editor in
      `builder/src/builder/panels/relations.rs`: per-pair grid row
      (faction A, faction B, public/secret attitudes, treaty, tension,
      trust/fear/rivalry) with click-to-select arming
      `BuilderState::relations_selected_pair`. The detail editor below the
      grid pins `public_attitude`, `secret_attitude`, `treaty_status`,
      `trust` / `fear` / `rivalry` / `ideological_distance` /
      `economic_dependency` / `military_pressure` / `covert_activity`
      (each with a per-axis "pin" checkbox + derived-value badge),
      `cause/reason` override, and the read-only tension scalar.

REL2. [DONE] Same cell editor exposes directional `a_to_b` / `b_to_a`
      views side-by-side; the `a_public_attitude`, `b_public_attitude`,
      `a_secret_attitude`, and `b_secret_attitude` combos under
      `RelationOverride` are editable independently and the derived
      attitudes are shown beneath each combo for reference.

REL3. [DONE] Kind-pair stance rules editor (`§REL3` collapsing header):
      one row per `KindRule` with kind A / kind B / stance ComboBox /
      cause; add/remove row buttons; commits live to
      `BuilderState::data_catalogs.relations.kind_rules`.

REL4. [DONE] Disposition delta rules editor (`§REL4`): one row per
      `DispositionRule` with disposition A / disposition B / `delta`
      DragValue (clamped -3..=3) / cause; add/remove buttons; commits to
      `data_catalogs.relations.disposition_rules`.

REL5. [DONE] Legacy `pair_overrides` editor (`§REL5`): list of pinned
      `(faction_id, faction_id, stance, cause)` rows with stance ComboBox
      and cause text edit; "+ add pin" row picks faction A / faction B /
      stance from the live `sector.factions` roster and dedupes by
      canonical pair on insert.

REL6. [DONE] Rich `RelationOverride` editor is the same cell editor as
      REL1 — every field under `RelationsConfig::overrides` is wired
      (public/secret attitudes symmetric + directional, treaty status,
      all 7 numeric dimensions, reason). "Clear override for pair" drops
      the row; the badge flips between "override pinned" /
      "derived (no override)" based on whether any field is set.

REL7. [DONE] `feed_conflict` checkbox in the §REL7 settings row. Edits
      flow to `data_catalogs.relations.feed_conflict` and recompute
      copies the flag onto `RelationsMatrix.feed_conflict` so the
      conflict tick reads the live value.

REL8. [DONE] `min_world_presence` slider (1..=10) bound to
      `state.config.generation.relations.min_world_presence`. Sliding
      the value arms `sectorforge.toml` dirty and (when auto-recompute
      is on) triggers `recompute_relations`, which forwards the
      threshold into `relations::derive_with_threshold`.

REL9. [DONE] "Recompute matrix" button in the header runs
      `BuilderState::recompute_relations` synchronously; the
      "auto-recompute on edit" checkbox (default ON) re-runs the same
      pass after every kind-rule / disposition-rule / pair-override /
      RelationOverride / settings mutation. The header also surfaces a
      "Save relations.toml" action that flushes the catalog through
      `project_io::save_project` (auto-pointing
      `config.inputs.relations` at `data/factions/relations.toml` when
      unset).

================================================================================
17. HISTORY (CHRONICLE)
================================================================================

H1.   [DONE] HISTORY tab `§H1 — chronicle config` grid in
      `builder/src/builder/panels/history.rs::show_config_section` binds
      `enabled`, `epoch_start`, `epoch_end`, `max_events_per_world`,
      `max_events_per_system`, `max_events_per_route`, `key_events_top_n`,
      and `max_subsector_events` to typed widgets driving
      `data_catalogs.history`. Edits arm dirty + auto-recompute when on.

H2.   [DONE] `show_eras_editor` renders one `egui::Frame::group` per
      `HistoryEra`: id / label text fields, relative_start / relative_end /
      weight `DragValue`s, and an inline allowed-events selectable strip
      keyed off `EVENT_KINDS`. `+ era` appends a fresh row with a
      synthesised id; `× remove` drops the row.

H3.   [DONE] `show_event_rules_editor` renders one row per
      `HistoryEventRule` with optional `id`, `when_system_state` ComboBox
      over `SystemState` variants (plus `(any)`), `prefer_event` ComboBox
      over `EventKind` (plus `(none)`), and `minimum_events` DragValue.
      Add / remove buttons wire to `data_catalogs.history.event_rules`.

H4.   [DONE] `show_events_editor` paints a striped grid (date / kind /
      anchor / weight / source / actions) backed by
      `sector.chronicle.events` and arms `BuilderState::
      selected_history_event`. The per-event inspector beneath the grid
      mutates date, kind, era_label, weight, summary, narrative, faction
      list, and consequences in place; every edit pins `manual = true` so
      the regen pass preserves it. `HistoryEvent` gains `manual: bool` in
      `src/history.rs` (skip_serializing_if = "not") so derived events stay
      compact on disk and only authored entries carry the flag.

H5.   [DONE] `show_add_event_wizard` opens a scratch `HistoryWizardState`
      (anchor kind + anchor pick + event kind + factions + optional date /
      narrative override). The wizard previews narrative live, lists
      suggested participating factions from the sector roster, and
      `Commit event` pushes a `HistoryEvent { manual: true }` through
      `BuilderState`.

H6.   [DONE] Header `Regenerate chronicle` button calls
      `BuilderState::recompute_chronicle` which drains the existing
      chronicle, splits derived from manual, re-runs
      `sectorforge::history::derive_with` against the live sector + the
      in-memory `HistoryConfig`, then appends every preserved manual event
      back onto the report and re-sorts by date. `auto-recompute on edit`
      toggle re-runs the same pass after every catalog edit.

H7.   [DONE] `show_timeline` renders the chronicle in date order with
      `date / kind / anchor / narrative-link / focus` per row. Clicking
      `focus` calls `focus_anchor` which sets the matching
      `selected_system_id` / `selected_world_id` / `selected_route_id` /
      `selected_region_id` / `selected_subsector_id` and switches to the
      relevant inspector tab. Clicking the narrative link arms the §H4
      inspector with the same event.

H8.   [DONE] WORLD inspector
      (`builder/src/builder/panels/world.rs::show_chronicle_section`)
      renders a `§H8 — Chronicle snippets (n)` collapsing block sourced
      from `panels::history::world_chronicle_events`. Each row shows date,
      event kind, manual badge, and narrative; `→ HISTORY` jumps to the
      HISTORY tab with that event focused.

================================================================================
18. PERSONAE (DRAMATIS)
================================================================================

PER1. [DONE] Per-faction-kind persona pool editor. Panel
      `builder/src/builder/panels/personae.rs::show_kind_pools_section`
      renders a collapsing header per built-in faction kind
      (`imperial / mechanicus / ecclesiarchy / inquisition / rogue_trader /
      chaos / rebel / necron / tyranid / ork / tau / aeldari / drukhari /
      harlequin / genestealer / xenos`) plus any custom kind the user
      authors via the "custom kind id" entry. Each kind exposes
      `name_prefixes / name_roots / name_suffixes / single_names / titles /
      traits` as comma-separated `text_edit_multiline` rows backed by
      `KindPools`. Empty pool fields fall back to the built-in defaults in
      `src/personae.rs::default_pool` via `merge_with_defaults`. The
      `[inputs].personae` field defaults to `data/personae.toml`; the
      catalog mirror lives on `BuilderState::data_catalogs.personae` and
      `recompute_personae` resolves it through the existing
      `personae::derive_with` entry point. Loaded by
      `project_io::catalogs_from_input` and round-tripped by
      `save_project_as` / `reload_catalog`.

PER2. [DONE] Per-anchor persona table + manual editor. Panel
      `show_persona_table` lists every derived persona by faction / kind /
      anchor (system slot or world id) / name / title / traits / agenda with
      per-row links that fire `BuilderState::focus_entity` so SYSTEM /
      WORLD / FACTIONS inspectors jump to the source. Manual entries are
      authored in `show_manual_editor` and written to
      `PersonaeConfig::manual`; `personae::derive_with` already appends them
      last so manual personae survive every regenerate pass.

PER3. [DONE] "Auto-derive personae" button calls
      `BuilderState::recompute_personae` (added in
      `state/derivations.rs`) which runs
      `sectorforge::personae::derive_with(&sector, &cfg)` and stashes the
      [`PersonaeReport`] on `BuilderState::personae_report` (personae is not
      part of `GeneratedSector`). The companion
      `personae_auto_recompute` toggle mirrors the §H6 / §REL9 pattern so
      catalog edits trigger an immediate recompute by default.

PER4. [DONE] Dominance-tier setting controls which presence anchors
      receive a persona. `show_dominance_section` exposes
      `min_world_dominance` over the `DominanceTier` enum (Presence /
      Influence / Contested / Controlled / Stronghold) alongside the
      `max_per_world` / `max_per_system` `DragValue` caps. All three are
      consumed by `personae::derive_with` on the next recompute.

PER5. [DONE] Agenda text bound to competing claims on the anchor world.
      The agenda string is produced by `personae::build_agenda` which
      reads `world.claims` to find a rival faction; the panel surfaces the
      derivation source as a tooltip beside each row
      (`kind = <faction_kind>`, `faction = <faction_id>`,
      `anchor = system <id> (<slot>)` or `world <system>/<world>`).

================================================================================
19. HOOKS (PLOT)
================================================================================

HK1.  [DONE] Hook list panel: ranked, filterable by kind (contested-claim,
      hidden-master, Perilous route, blockade, quarantine, warzone,
      Tyranid/Necron/Chaos pressure, StarvingWorld, LifelineLane).
      `builder/src/builder/panels/hooks.rs::show_filter_row` exposes a
      ComboBox over every `HookKind` variant; `show_hook_list` renders the
      cached `BuilderState::hooks_report` rows (`derive_with` already orders
      by descending dramatic weight) with the filter applied on top.

HK2.  [DONE] Per-hook details: anchor entity, prose, dramatic weight,
      GM-only flag. `show_detail_card` reads from
      `BuilderState::hooks_edit_target` / `selected_hook_id` and renders
      `id / kind / anchor / weight / gm-only / title / situation / stakes
      / factions / complications` in a two-column grid. Selecting a row in
      `show_hook_list` populates both fields so cross-tab links land here
      first-class.

HK3.  [DONE] Manual hook add: pick kind → pick anchor → write prose.
      `show_manual_editor` appends `HooksConfig::manual` entries via
      `+ manual hook` and offers per-entry editors for id / kind / anchor
      scope (System / World / Route) / anchor ids / title / situation /
      stakes / weight / GM-only / factions / complications.

HK4.  [DONE] "Regenerate hooks" runs `src/hooks.rs`. Manual hooks survive.
      `show_header_actions` exposes the "Regenerate hooks" button which
      calls `BuilderState::recompute_hooks` →
      `sectorforge::hooks::derive_with`. Manual entries survive because
      `derive_with` drops any derived hook sharing a manual id and then
      appends the whole `cfg.manual` block. `[inputs].hooks` -> `data/hooks.toml`
      mirrors the catalog through `project_io::save_project_as` /
      `reload_catalog` alongside the other catalogs.

HK5.  [DONE] Player-edition toggle (mirrors `--player`): hides GM-only hooks.
      `BuilderState::hooks_player_edition` is flipped via the checkbox in
      `show_header_actions`; `recompute_hooks` overrides
      `HooksConfig::hide_hidden_hooks` from this flag so the cached report
      already has `gm_only = true` rows stripped without re-running per
      frame.

HK6.  [DONE] Click-to-highlight anchor on map. Every anchor cell in
      `show_hook_list` and the "highlight on map" button on the detail
      card route through `focus_anchor` → `BuilderState::focus_entity`
      with the matching `EntityRef::System` / `World` / `Route`
      (falls back to `EntityRef::Tab(BuilderTab::Map)` when the anchor id
      is empty).

================================================================================
20. SITES
================================================================================

ST1.  [DONE] Per-world site editor: list of `{ kind, controller,
      public_status, actual_status, hook }`. Kind picker covers all 21
      (governor's palace, cathedral spire, manufactorum, underhive
      sump-city, void elevator, star-fort dockyard, quarantine zone,
      xenos ruin, pilgrim necropolis, astropathic choir, Arbites precinct,
      data-vault, disputed shrine, penal mine, black-market enclave, cult
      safehouse, crashed voidship, agri granary, forge reactor, tomb
      complex, naval anchorage). Implemented in
      `builder/src/builder/panels/sites.rs::show_site_list` /
      `show_detail_card` over the cached
      `BuilderState::sites_report` (published by
      `BuilderState::recompute_sites`); selecting a row stores
      `BuilderState::selected_site_id` /
      `BuilderState::sites_edit_target` and focuses the matching
      `EntityRef::World` so cross-tab links land first-class.

ST2.  [DONE] "Auto-derive sites" button calling
      `BuilderState::recompute_sites` runs
      `sectorforge::sites::derive_with(&sector, &cfg)` and stashes the
      result on `BuilderState::sites_report`. Manual sites survive
      because `derive_with` appends the whole `cfg.manual` block after
      the derived rows. Mirrors the personae/hooks pattern with an
      `sites_auto_recompute` toggle.

ST3.  [DONE] Player-edition toggle (`BuilderState::sites_player_edition`)
      flips `SitesConfig::player_edition` on every recompute so the
      derived rows where `public_status != actual_status` are stripped.
      The detail card hides the `actual_status` column under the same
      flag.

ST4.  [DONE] `data/sites.toml` editor: `show_config_section` binds
      `SitesConfig::max_per_world` and `SitesConfig::skip_uninhabited`;
      `show_manual_editor` edits `SitesConfig::manual` (id / kind picker
      across all 21 `SiteKind`s / system-world ids / name / controller /
      public + actual status pickers / known-to + tags CSV / hook prose).
      Project I/O wired in `project_io::catalogs_from_input` /
      `save_project_as` / `reload_catalog` so `data/sites.toml`
      round-trips alongside the other catalogs; `[inputs].sites` already
      lives on `src/config.rs::InputConfig` and `src/input.rs` parses
      the file into `ProjectInput::sites`; `synthesize_project_input`
      now feeds `data_catalogs.sites` to validation/regeneration instead
      of defaulting.

================================================================================
21. MISSIONS
================================================================================

M1.   [DONE] Mission list panel `builder/src/builder/panels/missions.rs`
      renders the cached `MissionsReport` published by
      `BuilderState::recompute_missions` (added in
      `state/derivations.rs`). Rows show kind across the full eight-kind
      `MissionKind` set (Investigate | Escort | Sabotage | Diplomacy |
      Assassination | Recovery | Defense | Exploration), title, patron,
      target, primary + secondary location, scale and a GM-only chip.
      A side detail card surfaces public objective, hidden complication
      (GM-only), reward, "if ignored" consequence, route ids, scale and
      visibility. Selecting a row populates `selected_mission_id` and
      `missions_edit_target` and focuses the matching `EntityRef::World`
      / `System` / `Route` so cross-tab links land first-class.

M2.   [DONE] `show_manual_editor` block edits
      `MissionsConfig::manual` with a kind picker over the full
      `KIND_VARIANTS` list, id / title / patron / target faction id /
      primary + secondary `sys[/world]` location / route-id CSV /
      objective / hidden / reward / consequence prose / scale +
      visibility pickers / weight DragValue / GM-only checkbox. New
      entries seeded with `blank_manual_mission` so each row starts with
      stable defaults.

M3.   [DONE] "Auto-derive missions" button calling
      `BuilderState::recompute_missions` runs
      `sectorforge::missions::derive_with(&sector, &cfg)` and stashes the
      result on `BuilderState::missions_report`. Manual missions survive
      because `derive_with` now extends the working list with
      `cfg.manual` after the per-anchor cap pass — sorted alongside the
      derived rows by `weight desc, id` so manual entries keep their
      authored priority. Mirrors the personae / hooks / sites pattern
      with a `missions_auto_recompute` toggle that re-runs the recompute
      whenever the catalog is edited.

M4.   [DONE] Player-edition toggle
      (`BuilderState::missions_player_edition`) flips
      `MissionsConfig::player_edition` on every recompute so the cached
      report drops `gm_only` rows. The list hides the "GM" column and
      the detail card hides the hidden-complication row under the same
      flag.

M5.   [DONE] Click-to-highlight. Every list row's "highlight" button,
      the detail card's primary / secondary location links and the
      route id links resolve through `focus_primary_location`, which
      parses the mission's `primary_location` string (`sys` or
      `sys/world`) into the matching `EntityRef::World` /
      `EntityRef::System` and dispatches `BuilderState::focus_entity`,
      landing on the right tab with the focus overlay set. When the
      location string is empty the call falls through to the first
      route id (when present) or the MAP tab.

      Project I/O wired in `project_io::catalogs_from_input` /
      `save_project_as` / `reload_catalog` so `data/missions.toml`
      round-trips alongside the other catalogs; `[inputs].missions`
      added to `src/config.rs::InputConfig` and `src/input.rs` parses
      the file into `ProjectInput::missions`;
      `synthesize_project_input` now feeds `data_catalogs.missions` to
      validation / regeneration instead of defaulting.

================================================================================
22. PROSE (GAZETTEER)
================================================================================

PR1.  [DONE] Per-system prose editor: derived prose displayed, "Override"
      toggle replaces with manual text.
      ([builder/src/builder/panels/prose.rs](../builder/src/builder/panels/prose.rs)
      `show_system_editor` — system picker (`ComboBox` seeded from the SYSTEM
      tab's selection via `selected_prose_system_id`, falling through to
      `selected_system_id` on first focus) plus an "Override" checkbox that
      flips `ProseConfig::overrides::systems` for the chosen `SystemId`. The
      first toggle seeds the override with `entry.paragraphs.join("\n\n")`
      so the user edits in place; "Revert to derived" removes the entry. The
      derived paragraphs stay cached on
      [`SystemProse::derived_paragraphs`](../src/prose.rs) inside the active
      report so the "Derived paragraphs (read-only)" collapsing block can
      restore the original text without re-running derivation. Overrides
      survive every "Regenerate prose" because they live inside
      `data_catalogs.prose` and `prose::derive_with` re-applies them after
      the deterministic derivation.)

PR2.  [DONE] Per-sector overview prose editor (same pattern).
      (`show_overview_editor` mirrors §PR1 against
      `ProseConfig::overrides::overview`. `ProseReport::overview_is_override`
      reflects the toggle state so the panel surfaces an "AUTHORED" badge
      next to the override; blank / whitespace-only overrides fall back to
      the derived overview at derive time.)

PR3.  [DONE] Tone preset selector: Florid | Administratum Dispatch.
      (`show_tone_section` exposes a `ComboBox` over
      [`ProseTone::Gazetteer`] / [`ProseTone::Dispatch`] bound to
      `ProseConfig::tone`, plus include-overview / include-per-system
      checkboxes that mirror the CLI knobs. Changing the tone rewrites the
      derived paragraphs on the next recompute; overrides are untouched
      because they store the manual text verbatim.)

PR4.  [DONE] "Regenerate prose" runs `src/prose.rs`. Manual overrides
      survive. (`show_header_actions` exposes the "Regenerate prose" button
      which calls `BuilderState::recompute_prose` — added in
      [builder/src/builder/state/derivations.rs](../builder/src/builder/state/derivations.rs).
      The button runs [`prose::derive_with`](../src/prose.rs) against the
      live sector + `data_catalogs.prose`, then publishes the
      [`ProseReport`](../src/prose.rs) onto `BuilderState::prose_report`. A
      `prose_auto_recompute` toggle (default `true`) re-runs the derivation
      on every catalog edit through `on_catalog_edited`. Manual overrides
      survive because `prose::derive_with` re-applies the
      [`ProseOverrides`](../src/prose.rs) block after the deterministic
      derivation pass — see the four tests in `src/prose.rs` and the four
      tests in `builder/src/builder/panels/prose.rs`. Project I/O wired in
      `project_io::catalogs_from_input` / `save_project_as` /
      `reload_catalog` so `data/prose.toml` round-trips alongside the other
      catalogs; `[inputs].prose` added to `src/config.rs::InputConfig` and
      `src/input.rs` parses the file into `ProjectInput::prose`;
      `synthesize_project_input` feeds `data_catalogs.prose` to validation
      / regeneration instead of defaulting. The `sectorforge prose` CLI now
      honours `[inputs].prose` when `--project` is supplied so authored
      overrides survive both the builder and the CLI path.)

================================================================================
23. BRIEFING
================================================================================

BR1.  [DONE] Preset picker: gm | navy | inquisition | trader | governor | public.
      `builder/src/builder/panels/briefing.rs::show_preset_row` exposes a
      `ComboBox` over the six `sectorforge::briefing::AudiencePreset`
      variants (`GmFullTruth` / `ImperialNavy` / `Inquisition` /
      `RogueTrader` / `LocalGovernor` / `PublicAtlas`). The selected
      variant lives on `BuilderState::briefing_preset` (default
      `GmFullTruth`); flipping it calls `invalidate_preview` so the next
      "Generate briefing" pass rebuilds against the new audience instead
      of showing a stale Markdown body.

BR2.  [DONE] Observer-faction picker (optional).
      `show_observer_row` enumerates `state.sector.factions` into a
      `ComboBox` whose value is stored on
      `BuilderState::briefing_observer: Option<FactionId>` (default
      `None`). When `Some`, `build_profile` sets
      `BriefingProfile::observer_faction` so `briefing::apply` retains
      only that observer's intel sub-record on each system and runs the
      observer-visibility floor through `min_confidence`. When `None`,
      every non-observer intel record is dropped.

BR3.  [DONE] `min_confidence` slider (0..=100).
      `show_confidence_row` binds `BuilderState::briefing_min_confidence`
      (default 30, matches `BriefingProfile::default`) to a 0..=100
      `egui::Slider`. `effective_min_confidence` reapplies the
      preset-baked floor (e.g. `PublicAtlas` clamps to ≥80,
      `Inquisition` clamps to ≤20) and the panel surfaces the
      post-clamp value next to the slider so the GM sees what the
      profile will actually enforce.

BR4.  [DONE] "Generate briefing" writes redacted clone via
      `src/briefing.rs`. Preview the redacted Markdown in a side panel.
      `show_generate_row` -> `regenerate_preview` builds a
      `BriefingProfile` via `build_profile`, calls
      `sectorforge::apply_briefing` (the public alias for
      `briefing::apply`) and stores the returned `BriefingPack` on
      `BuilderState::briefing_preview_pack` plus the rendered Markdown
      from `briefing::render_markdown` on
      `BuilderState::briefing_preview_md`. `show_preview` renders the
      Markdown verbatim in a scrollable monospace `TextEdit::multiline`
      so the GM can review the redacted output before exporting. Any
      change to preset / observer / confidence calls
      `invalidate_preview` so the preview can't drift away from the
      controls.

BR5.  [DONE] Export both `.md` + `.json` to chosen folder.
      `show_export_row` pairs "Choose export folder…" (a `rfd`
      `FileDialog::pick_folder` that stamps
      `BuilderState::briefing_export_dir`) with "Export .md + .json"
      (enabled only when both the folder and the cached pack exist) that
      calls `sectorforge::write_briefing(dir, &pack, &profile)`. The
      writer emits `briefing-<profile_id>.md` and
      `briefing-<profile_id>.json`; success and failure both surface
      through `ModalKind::Message`. The folder selection persists across
      generations so a "tweak → export" loop needs only one folder pick.

================================================================================
24. INTERESTINGNESS
================================================================================

INT1. [DONE] Profile picker: political_sandbox | grim_collapse | mercantile |
      villainous | frontier.
      - `builder/src/builder/panels/interestingness.rs::show_profile_row`
        renders a `ComboBox` over the five built-in
        `sectorforge::interestingness::ProfileId` variants and writes the
        selection back to `BuilderState::interestingness_profile`
        (default `ProfileId::PoliticalSandbox`).
      - Switching the profile clears
        `BuilderState::interestingness_report` and
        `interestingness_custom_pick`, so the next §INT2 pass rebuilds
        against the new bands and the §INT4 picker resets its scratch
        selection. Per-profile override tables in
        `interestingness_custom_overrides` survive the switch.

INT2. [DONE] "Score sector" runs `src/interestingness.rs`. Shows 0..=100 score
      + strengths/weaknesses lines.
      - `show_score_row` calls a local `rescore()` helper that builds an
        `InterestingnessConfig` from the live profile +
        `interestingness_custom_overrides` and runs
        `sectorforge::derive_interestingness_with`. The cached
        `InterestingnessReport` lives on
        `BuilderState::interestingness_report`.
      - The "Overall: N / 100" badge is colour-coded (green ≥75,
        amber ≥50, red otherwise). Strengths / weaknesses lists are
        rendered straight from the report.

INT3. [DONE] Per-metric bar chart with target band shaded.
      - `show_metrics_chart` walks `metric_scores` and calls
        `draw_metric_row`, which allocates an 18-pixel-tall track
        (`ui.allocate_exact_size` + `ui.painter_at`) and paints:
          * a dark-grey background spanning `[0, vmax]` where
            `vmax = max(target_high * 1.5, observed * 1.2, 1.0)`,
          * a green band over `[target_low, target_high]`,
          * a white tick at `observed`,
          * a 1-pixel border.
      - The right-hand text reads
        `obs … · band … · fit …% · w …` and inherits the same
        green/amber/red colour ramp as the overall badge.

INT4. [DONE] Threshold override per profile (custom profile editor).
      - `BuilderState::interestingness_custom_overrides` is a
        `BTreeMap<String, BTreeMap<String, MetricTarget>>` keyed by the
        snake-case profile id (`political_sandbox`, `grim_collapse`,
        `mercantile`, `villainous`, `frontier`) so each profile keeps
        its own table; switching profile + back leaves the overrides
        intact.
      - `show_add_override_row` exposes a `ComboBox` over a fixed
        `METRIC_CATALOG` covering every metric the library's
        `observed_metrics` table emits (`faction_gini`,
        `contested_world_ratio`, `avg_claims_per_world`,
        `route_components`, `articulation_points`, `isolated_systems`,
        `route_diameter`, `world_type_diversity`, `warzone_count`,
        `blockaded_count`, `infiltrated_count`,
        `asymmetric_control_worlds`, `faction_count`). Already-overridden
        metrics are filtered out.
      - "Add" seeds the new entry from the profile's built-in band via
        `seed_target`, which queries the library through a one-shot
        `derive_interestingness_with` so the seed never drifts from the
        scorer's source of truth.
      - Existing override rows expose `DragValue` editors for
        `low / high / floor / ceil / weight`. An `∞ / set finite ceil`
        toggle handles the `MetricTarget::ceil = f32::INFINITY` case
        without breaking the `DragValue` widget. "Remove" drops the
        override (and prunes the per-profile sub-map when it goes
        empty), reverting that metric to the built-in band.
      - Any add / edit / remove clears
        `interestingness_report` so the chart never lies about which
        bands fed the last score.

================================================================================
25. ANALYTICS DASHBOARD
================================================================================

A1.   Full `src/analytics.rs` parity: faction Gini, per-faction
      projection share, contested-world ratio, average claims per world,
      claim-kind + dominance counts, world-type / star-colour /
      population distributions, route-graph stats (component count,
      diameter, articulation points, isolated systems), per-subsector
      political variety, health-flag list.

A2.   `[analyze]` config editor: warn_faction_share, warn_if_disconnected,
      warn_if_articulation, warn_contested_ratio, tiny_sector_threshold.

A3.   "Strict" toggle (treats health flags as errors for CI parity).

A4.   Export `analysis.md` + `analysis.json`.

================================================================================
26. SEARCH (WISHES)
================================================================================

SR1.  Wishes editor reaches every constraint kind from `src/search.rs`:
        faction_share_min/max
        faction_world_count_min/max
        faction_system_count_min/max
        world_type_exists
        contested_world_min (with n_way), contested_world_max
        system_state_count_min/max
        route_graph_connected
        no_articulation_points
        diameter_max
        isolated_systems_max
        contested_ratio_min/max
        stance_count_min/max (per stance bucket)
        region_count_min/max (per region kind)
        economy_stranded_max
        economy_resource_min
      Each constraint kind has its own form widget.

SR2.  Run-search button. Off-thread enumeration. Progress bar shows
      n_candidates_tried, n_passed, best_near_miss_score.

SR3.  Result panel: winning seed (Apply button) + top N near-miss
      candidates (View + Apply).

SR4.  `base_seed`, `budget`, `report_top` fields.

SR5.  Validation: faction id existence preflight before run.

================================================================================
27. DIFF + TICKS
================================================================================

DF1.  Two-sector diff: pick before/after `sector.json` (or take a
      snapshot of current sector as either).

DF2.  Tick-mode: simulate N conflict ticks from current sector, then
      diff. Re-uses `src/conflict.rs::advance_sector`.

DF3.  Diff panel: tree of strata (systems, routes, factions, regions,
      economy, relations), click-to-expand per-entity changes.

DF4.  Filters: skip-worlds, skip-routes, min_faction_delta.

DF5.  Export `diff.md` + `diff.json`.

================================================================================
28. CONFLICT + STABILITY
================================================================================

CF1.  Per-world conflict editor: momentum, intensity, mobilisation,
      attacker, defender, visible_controller.

CF2.  Per-system conflict view: aggregated from worlds; "Override
      aggregate" toggle for manual edits.

CF3.  Per-world stability editor: 7 dimensions (public_order, corruption,
      fear, rebellion, xenos_threat, warp_instability, famine).

CF4.  "Advance N ticks" button runs `src/conflict.rs::advance_sector`.
      Hysteresis preserved.

CF5.  Tick log: chronological list of momentum / state changes.

CF6.  Conflict heatmap option in §35.

================================================================================
29. INTEL + VISIBILITY
================================================================================

I1.   [DONE] Per-system intel editor lives under the SYSTEM tab as
      `§I1 — Intel / fog of war` (see
      `builder/src/builder/panels/intel.rs::show_system_intel_section`).
      Each observer faction id maps to an `ObserverView` row with
      `last_verified_tick`, confidence (0..=100), propaganda + classified
      state combos, and a nested `suspected_presences` list (faction id,
      `IntelSource`, confidence). Backing store is
      `GeneratedSystem::intel.by_observer`.

I2.   [DONE] Per-world intel editor mirrors §I1 under the WORLD tab as
      `§I2 — Intel / fog of war`. Same `ObserverView` editor + redaction
      preview, scoped to a new `GeneratedWorld.intel: SystemIntel` field
      added in `src/sector_model/mod.rs`. `src/intel.rs::derive_world_intel`
      builds the per-world observer view; both layers serialise with
      `skip_serializing_if = "SystemIntel::is_empty"` so omniscient
      sectors stay compact on disk.

I3.   [DONE] "§I3 Generate baseline intel" button on the MAP / SYSTEM /
      WORLD intel sections calls `sectorforge::intel::derive_intel` over
      the live sector with every distinct `sector.factions[i].id` as the
      observer set. Pre-existing observer records are overwritten. Marks
      the project dirty + arms validation.

I4.   [DONE] Observer-faction lens on the MAP tab via
      `show_map_intel_controls`. The combo writes
      `BuilderState::intel_observer: Option<FactionId>` (`None` =
      omniscient). The §I2 redaction preview consumes the lens through
      `sectorforge::intel::redact_world_for_observer` so hidden presences
      are dropped from the per-world readout in real time.

I5.   [DONE] `player_min_confidence` slider on the same MAP control row,
      bound to `BuilderState::intel_player_min_confidence: u8` (0..=100,
      default 0). Cutoff feeds `redact_world_for_observer` so the
      WORLD-tab redaction preview matches the `--player` export gate.

================================================================================
30. ARCHETYPES
================================================================================

AR1.  [DONE] Per-system archetype editor in the SYSTEM tab
      (`builder/src/builder/panels/system.rs::show_archetype_section`)
      covers every `ArchetypeState` field: `imperial_co_sovereigns`
      chip-row with faction picker + × remove, `necron_phase` /
      `tyranid_stage` / `gsc_stage` / `tau_sphere` ComboBoxes, and
      `ork_waaagh` / `aeldari_activity` / `chaos_corruption` /
      `daemon_manifestation` sliders 0..=100. Flavour notes live in the
      existing Tags / Notes section. Edits route through
      `BuilderCommand::SetArchetype { system, before, after }` so undo
      restores the prior `ArchetypeState`; a "Reset to default" button
      pins `ArchetypeState::default()` and an "Auto-assign from sector
      data (this system only)" button runs `archetypes::apply_all` over
      a scratch clone of the sector and applies the §AR3 mask before
      copying the result back into the live system. Round-trip covered
      by `set_archetype_round_trip`.

AR2.  [DONE] "Auto-assign archetypes" sector-wide button lives in
      `show_archetype_auto_assign`. Dispatches
      `BuilderCommand::AutoAssignArchetypes { flags, before }`, which
      snapshots every `(SystemId, ArchetypeState)` before running
      `sectorforge::archetypes::apply_all` (the canonical entry — no
      separate `derive` exists) and then masks each per-axis result via
      the §AR3 flags. Revert restores the snapshot in one pass. Covered
      by `auto_assign_archetypes_round_trip_respects_flag_mask`.

AR3.  [DONE] Archetype rules editor in `show_archetype_rules`.
      `src/archetypes.rs` ships no TOML config layer, so this is a
      builder-app-only defaults panel: per-axis enable checkboxes (8
      axes mirroring §16.1..§16.12) plus "Enable all" / "Disable all"
      buttons. State lives on `BuilderState::archetype_flags:
      ArchetypeApplyFlags` (declared in `builder/src/builder/command.rs`)
      and is never serialised into `sector.json`. Disabled axes are
      reset to `ArchetypeState::default()` values after §AR2 runs.

================================================================================
31. ORBITAL ASSETS + BLOCKADE
================================================================================

O1.   [DONE] Per-system orbital-asset editor: list of
      `{ kind (Station|Shipyard|DefensePlatform|BlockadeFleet),
         faction, capacity, notes }`. Implemented in
      `builder/src/builder/panels/orbital.rs` as `show_orbital_section`,
      called from `panels/system.rs`. Each row exposes a `kind`
      `ComboBox`, faction `ComboBox` over the sector's faction list, a
      `strength` slider (0..=100), an inline `ship_inventory` editor
      (rows of `hull_class`/`count`), and × delete. Add / Derive /
      Clear buttons sit at the section footer. Mutations flow through
      `BuilderCommand::SetOrbitalAssets { system, before, after }` so
      every edit is undoable.

O2.   [DONE] Blockade report editor: aggregated `BlockadeReport`
      per system. The same `panels/orbital.rs` renders an inline
      `Blockade` block: `under_blockade` checkbox, `blockader` /
      `besieged` faction pickers (`(none)` clears), `intensity`
      0..=100 slider. The "Derive now" button calls
      `sectorforge::orbital_assets::derive_orbital_assets` for the
      system and writes both the assets list and the report. Manual
      overrides round-trip through
      `BuilderCommand::SetBlockadeReport { system, before, after }`.
      The §S2 overlays section in `panels/system.rs` jumps focus into
      this section so the existing summary read-out is now a deep link.

================================================================================
32. SURFACE REGIONS
================================================================================

SU1.  [DONE] Per-world surface-region editor: list of
      `{ kind (Capital|Hive|Underhive|ForgeComplex|ShrineContinent|...),
         dominant_faction, notes }`. Panel
      `builder/src/builder/panels/surface_regions.rs` renders a
      collapsing section under the WORLD tab. Each row exposes name,
      kind ComboBox over the 12 `RegionKind` variants, optional
      dominant FactionId combo, control_score / population_weight /
      visibility sliders (0..=100), and a multi-line notes box. The
      free-form `notes: String` field was added to
      `src/surface_region.rs::SurfaceRegion` with
      `#[serde(default, skip_serializing_if = "String::is_empty")]`
      so existing JSON parses unchanged. Mutations route through
      `BuilderCommand::SetSurfaceRegions { world, before, after }` so
      every edit is undoable; round-trip covered by
      `set_surface_regions_round_trip` in
      `builder/src/builder/command.rs`. Population-weight sum > 100
      surfaces a yellow over-allocation pill.

SU2.  [DONE] Auto-seed surface regions from world type via
      `src/surface_region.rs::derive_regions` — already invoked by
      `src/generation/mod.rs` (world-build path) so freshly generated
      worlds have regions populated. The §SU1 panel exposes an
      "Auto-seed (§SU2)" button that calls `derive_regions` and
      replaces the current list, plus a "Clear regions" button.

================================================================================
33. VALIDATION + INVARIANTS SURFACE
================================================================================

V1.   [DONE] Validation panel (`src/validation.rs::validate_project`):
      `builder/src/builder/panels/validation.rs` groups
      `ValidationReport.errors` / `warnings` under collapsing per-file
      buckets keyed by the issue `path` prefix (`factions`, `routes`,
      `relations`, `regions`, `economy`, `history`, names, fall back to
      `sectorforge.toml`). Each leaf is a button that sets
      `BuilderState::selected_file` so the §P4 project tree (and Phase
      E TOML tabs) can jump to the offending file. The footer shows the
      world-workbook stats. A "Re-validate now" button forces an
      immediate `BuilderState::revalidate_now` flush.

V2.   [DONE] Invariants panel (`src/invariants.rs::check_sector`):
      `builder/src/builder/panels/invariants.rs` groups
      `InvariantReport.violations` by stratum (systems / worlds /
      routes / factions / regions / economy / manifest / other) using
      the invariant `path` prefix. Each row is a button that writes
      typed IDs into the `BuilderState::selected_*` mailboxes
      (`selected_system_id`, `selected_world_id`, `selected_route_id`,
      `selected_faction_id`, `selected_region_id`) so the upcoming
      inspector tabs can focus the offending entity. The panel also
      exposes the §V5 invariant catalogue as a read-only collapsing
      list so users can audit what is checked on clean sectors.

V3.   [DONE] Live validation on every mutation, debounced.
      - `BuilderState::run` / `undo` / `redo` call `mark_validation_dirty()`
        (stamps `Instant::now` into `validation_dirty_since`).
      - The UI loop calls `BuilderState::pump_validation()` per frame; once
        `validation_debounce` (default 200 ms,
        `DEFAULT_VALIDATION_DEBOUNCE_MS`) elapses, `revalidate_now()` fires.
      - `revalidate_now()` builds a synthetic `ProjectInput` via
        `synthesize_project_input()` (worlds catalog mandatory; other
        catalogs default-fall-through) and stores the result in
        `validation_report`. The debounce timer is cleared even when the
        worlds catalog is absent so we don't re-arm every tick.
      - Invariants continue to run synchronously inside the command bus.
      - Status-bar indicator is now tri-coloured via
        `BuilderState::health_level()`:
          * RED — any validation error or invariant violation
          * YELLOW — warnings, or either report not yet computed
          * GREEN — both reports present and clean
      - Tests in `state.rs`: `mutation_arms_validation_debounce`,
        `pump_validation_holds_within_debounce_window`,
        `pump_validation_flushes_after_debounce`,
        `revalidate_now_populates_report_when_worlds_present`,
        `health_level_red_on_invariant_violation`,
        `health_level_yellow_when_reports_missing`,
        `health_level_green_when_both_clean`.

V4.   Strict-mode toggle (warnings → errors).

V5.   Invariant catalogue list (read-only) so user can see what's checked:
        SYSTEM_ID_DUPLICATE, WORLD_ID_DUPLICATE, COORD_OUT_OF_BOUNDS,
        COORD_DUPLICATE, ROUTE_ENDPOINT_MISSING, ROUTE_DISTANCE_MISMATCH,
        DUPLICATE_UNDIRECTED_EDGE, FACTION_SUMMARY_INCOHERENT,
        WORLD_TAG_NAMESPACE_MISSING, MANIFEST_COUNT_MISMATCH,
        REGION_HEX_OUT_OF_BOUNDS, REGION_HEX_OVERLAP,
        REGION_ISOLATES_SECTOR, ECONOMY_ENABLED_NO_WORLDS, ...

V6.   Pre-export refuse-on-error gate (mirrors CLI `generate`).

================================================================================
34. EXPORT BUNDLE
================================================================================

EX1.  Export dialog covers every format from `[outputs]`:
      json, markdown, bitmap, html. Per-format toggle.

EX2.  Manifest writer (`src/export.rs::write_manifest`) included.
      Input digests computed over current in-memory data catalogs.

EX3.  Bitmap settings panel: `sector_scale` (1..=8), `system_scale`
      (1..=8), `render_systems` toggle, `faction_fill` toggle,
      `heatmap` mode picker.

EX4.  HTML settings panel: theme picker (dark | parchment | hololithic),
      `player_observer` picker, `player_min_confidence`,
      `size_warn_bytes`, `compact_json`.

EX5.  Standalone-system export (mirrors `generate-system`): pick coord
      + seed + index → write `sys-NNNN.json` (+ optional `.md`).

EX6.  "Render markdown" preview tab (live, mirrors `render-markdown`
      output).

EX7.  Per-overlay export buttons: history (`history.md/json`),
      personae, hooks, prose, relations, regions, economy,
      interestingness, briefing, missions, sites, analyze.

EX8.  Project-wide "Export everything" runs every overlay and writes
      to chosen folder.

================================================================================
35. THEMES + HEATMAPS
================================================================================

T1.   Theme picker covers all built-ins:
      gm_dark | print_mono | imperial_archive | navis_tactical |
      inquisition_redacted | subsector_political.

T2.   Custom theme editor (`data/map_themes/<name>.toml`):
      background, route_line_mode (standard | hazard_weighted),
      label_density (all | important_only | none),
      legend (full | compact | hidden),
      symbol_set (standard | tactical | redacted), per-element colours.

T3.   Heatmap selector covers every mode:
      off | control | military | trade | industrial | covert | faith |
      threat | intel | tension | trade_volume | food_output |
      tithe_stress | supply_vulnerability | conflict_intensity |
      stability (per-dimension).

T4.   Route-style legend live-updates on theme change.

T5.   Per-system bitmap preview tab.

================================================================================
36. SEGMENTUM COMPOSE
================================================================================

SG1.  Segmentum editor: grid layout (columns × rows), per-child entry
      `{ id, project_dir, column, row, seed_override }`,
      `faction_mode` (shared | independent), `stitch_seed`,
      `[stitch]` block (max_links_per_pair, border_depth,
      default_route_type, default_stability).

SG2.  "Compose" runs `src/segmentum.rs::compose_segmentum` off-thread.
      Progress shown per child (load, validate, generate, invariant,
      export, stitch).

SG3.  Super-manifest preview after compose.

SG4.  Per-link editor on stitch step: add/remove/edit inter-sector
      warp links.

SG5.  Open composed segmentum in existing SEGMENTUM tab.

================================================================================
37. PROJECT + FILES (operational)
================================================================================

PF1.  Project tree panel (§P4).

PF2.  TOML editor tabs for every config file with syntax highlighting +
      validation (`sectorforge.toml`, `factions.toml`, `relations.toml`,
      `route_rules.toml`, `regions.toml`, `economy.toml`, `history.toml`,
      `system_names.toml`, `world_names.toml`, `wishes.toml`,
      `map_theme.toml`, `segmentum.toml`, `personae.toml`, `sites.toml`).

PF3.  `worlds.toml` editor: typed dropdowns + DragValue weights
      (already exists; extend with delete-row, insert-row, validation surface).

PF4.  Dirty-state markers per tab.

PF5.  Single "Save all" + per-tab "Save".

PF6.  Atomic write via `tempfile::NamedTempFile::persist`.

================================================================================
38. UNDO + SNAPSHOTS
================================================================================

U1.   [DONE] Command-pattern: every mutation goes through `BuilderCommand`
      (D5). `apply` / `revert` implemented per variant in
      `builder/src/builder/command.rs`. Round-trip tests
      (`add_system_round_trip`, `remove_system_round_trip`) plus
      `command_log_determinism_blake3` (R8) cover the contract.

U2.   [DONE] Ctrl-Z / Ctrl-Y bindings for undo / redo. Bounded ring buffer
      (default 200 commands; configurable).
      - `BuilderState::command_log_capacity` (default
        `DEFAULT_COMMAND_LOG_CAPACITY = 200`, `0 = unbounded`).
      - `BuilderState::run` calls `enforce_command_log_capacity`, which
        drains the oldest commands and shifts `command_cursor` plus every
        `Snapshot::command_log_position` by the drop-count so undo / redo
        / snapshot references stay coherent.
      - `builder/src/builder/panels/shortcuts.rs::handle(ctx, state)` consumes
        `Ctrl-Z` (undo), `Ctrl-Y` and `Ctrl-Shift-Z` (redo) via
        `egui::Context::input_mut(|i| i.consume_shortcut(...))` so other
        widgets do not also see the chord. Errors surface as
        `ModalKind::Message`.
      - Tests in `state.rs`: `ring_buffer_caps_command_log`,
        `ring_buffer_shifts_snapshot_positions`,
        `unbounded_capacity_zero_keeps_all_commands`,
        `default_capacity_is_200`, `undo_redo_basic_round_trip`,
        `undo_clamps_at_zero`.

U3.   Named snapshots: "Snapshot" button captures current sector +
      command-log position. Snapshots listed in history tab.

U4.   "Revert to snapshot" rolls sector back. Commands after the
      snapshot are discarded with confirmation.

U5.   Snapshot diff vs. current (uses §27).

U6.   Auto-snapshot on destructive batch ops (e.g., recluster
      subsectors).

================================================================================
39. LIVE DERIVATIONS
================================================================================

LD1.  Per-derivation cache keyed by BLAKE3 over the relevant sector
      slice + config + version. Cache entries:
      analytics, relations, regions, economy, history, personae, hooks,
      prose, sites, missions, briefing, interestingness, subsectors,
      power_projection, influence_field, intel.

LD2.  On mutation, invalidate caches whose key inputs changed (precise
      invalidation, not full flush). Use a dependency table:
        systems/worlds → all
        factions → analytics, relations, personae, hooks, sites,
                   missions, briefing, intel, power_projection,
                   influence_field
        regions → routes (stability recompute), economy (anomaly
                   weighting), invariants
        routes → analytics, economy, hooks
        relations.toml → relations, briefing, conflict
        economy.toml → economy, hooks (lifeline, starving)

LD3.  Background re-derivation thread: when cache invalidated, kicks off
      recompute; UI shows stale-tag until refresh completes.

LD4.  All overlay panels (history, relations, regions, economy,
      analytics, hooks, sites, missions, prose, personae, briefing,
      interestingness) consume the live cache, not stored sector fields.

================================================================================
40. TESTING + DETERMINISM
================================================================================

T1.   Unit tests per `BuilderCommand` variant: apply → revert →
      sector equal to start.

T2.   Round-trip: `BuilderState` → save `.sgforge` → load → equal.

T3.   Determinism golden: scripted command sequence → final sector
      JSON → BLAKE3 stable across runs.

T4.   Proptest fuzz over `BuilderCommand` sequences; every state must
      pass `validate_project` + `check_sector`.

T5.   Integration test: blank sector → 50 commands → export project →
      run CLI `generate` over exported project → output equals
      builder's snapshot.

T6.   Builder GUI smoke tests via `egui_kittest` (if added) or manual
      harness. Manual command:
        `cargo run -p sectorforge-builder -- --project <dir>`
      Confirm: builder loads, workspace tab renders, PROJECT/MAP/SYSTEM/WORLD
      tabs paint, save succeeds without panic.

T6b.  Viewer handoff smoke test:
        a) save the project in `sectorforge-builder`
        b) open the same directory with `cargo sview --project <dir>`
      Confirm: `sectorforge-viewer` loads the saved `out/sector.json` through
      its normal viewer/editor path. Do not embed builder UI in the viewer
      to satisfy this test.

T7.   Benchmarks (criterion) for large-sector mutations (>= 200
      systems, >= 800 worlds): command apply < 1 ms, full re-validate
      < 50 ms, full re-derive < 500 ms cold / < 50 ms warm.

================================================================================
41. UI ROUTING / NAV
================================================================================

N0.   [DONE] Separate app shell. `builder/src/main.rs` owns the
      `sectorforge-builder` CLI and creates `BuilderApp`; `builder/src/app.rs`
      owns `BuilderWorkspace`, workspace chips, top tab strip, active-panel
      dispatch, status bar, modal layer, watcher drain, shortcut handling,
      and live-validation pumping. It does not reuse `sectorforge_viewer::App`
      and does not mount inside `sectorforge-viewer`.

N1.   [DONE] Top-level tabs: PROJECT | MAP | SYSTEM | WORLD | FACTIONS |
      CONTROL | REGIONS | ROUTES | SUBSECTORS | ECONOMY | RELATIONS |
      HISTORY | PERSONAE | HOOKS | SITES | MISSIONS | PROSE |
      ANALYTICS | INTERESTINGNESS | SEARCH | DIFF | BRIEFING |
      SEGMENTUM | EXPORT.
      `builder/src/builder/state/types.rs::BuilderTab` enumerates all 24 tabs;
      `BuilderTab::ALL` is the canonical ordering used by the top tab
      strip. Active tab persists on `BuilderState::active_tab` (defaults
      to `Project`). Tests `default_tab_is_project`,
      `builder_tab_all_is_full_n1_set`, and
      `builder_tab_labels_are_uppercase_words` pin the contract.

N2.   [DONE] Each tab corresponds to a panel module under
      `builder/src/builder/panels/` and follows the R10 contract.
      `builder/src/builder/panels/nav.rs::show_top_bar` renders the strip;
      `show_active_panel` dispatches `BuilderTab` -> panel module:
        Project   -> panels/project.rs    (composes §P1..§P6 surfaces)
        Map       -> panels/map.rs        (hosts the §N3 toolbox)
        System    -> panels/system.rs     (Phase B §S1..§S6 DONE)
        World     -> panels/world.rs      (Phase B §W1..§W7 DONE)
        Factions  -> panels/factions.rs   (Phase B §F1..§F7 placeholder)
        Control   -> panels/control.rs    (Phase B §C1..§C8 placeholder)
        Regions   -> panels/regions.rs    (Phase C §REG1..§REG7 DONE)
        Routes    -> panels/routes.rs     (Phase B §R1..§R7 DONE)
        Subsectors -> panels/subsectors.rs (Phase C §SUB1..§SUB5 DONE)
        Economy   -> panels/economy.rs    (Phase C §E1..§E7 DONE)
        Relations -> panels/relations.rs  (Phase C §REL1..§REL9 DONE)
        History   -> panels/history.rs    (Phase C §H1..§H8)
        Personae  -> panels/personae.rs   (Phase D §PER1..§PER5)
        Hooks     -> panels/hooks.rs      (Phase D §HK1..§HK6)
        Sites     -> panels/sites.rs      (Phase D §ST1..§ST4)
        Missions  -> panels/missions.rs   (Phase D §M1..§M5)
        Prose     -> panels/prose.rs      (Phase D §PR1..§PR4)
        Analytics -> panels/analytics.rs  (Phase E §A1..§A4)
        Interestingness -> panels/interestingness.rs (Phase D §INT1..§INT4 DONE)
        Search    -> panels/search.rs     (Phase E §SR1..§SR5)
        Diff      -> panels/diff.rs       (Phase E §DF1..§DF5)
        Briefing  -> panels/briefing.rs   (Phase D §BR1..§BR5 DONE)
        Segmentum -> panels/segmentum.rs  (Phase E §SG1..§SG5)
        Export    -> panels/export.rs     (Phase E §EX1..§EX8)
      Stub panels share `panels/placeholder.rs` so each unfinished body
      announces the phase + section that fills the tab.

      Outstanding panels (ground-truth tracker for STUB-001; each stub file
      carries a matching `// TODO(docs/BUILDER_REQS.txt §X.Y): implement`
      header so `rg "TODO.docs/BUILDER_REQS"` enumerates the same set):

        Phase D
          [x] panels/briefing.rs        §BR1..§BR5  briefing editor
          [x] panels/hooks.rs           §HK1..§HK6  hook editor
          [x] panels/interestingness.rs §INT1..§INT4 scorer
          [x] panels/missions.rs        §M1..§M5    mission editor
          [x] panels/personae.rs        §PER1..§PER5 persona editor
          [x] panels/prose.rs           §PR1..§PR4  prose editor
          [x] panels/sites.rs           §ST1..§ST4  site editor

        Phase E
          [ ] panels/analytics.rs       §A1..§A4    analytics dashboard
          [ ] panels/diff.rs            §DF1..§DF5  diff + tick editor
          [ ] panels/export.rs          §EX1..§EX8  export bundle editor
          [ ] panels/search.rs          §SR1..§SR5  wishes/search editor
          [ ] panels/segmentum.rs       §SG1..§SG5  compose editor

      `panels/placeholder.rs` is the shared 13-LOC routing-fallback helper
      every stub above calls — it is NOT a stub itself and stays after the
      last Phase D/E panel above lands.

N3.   [DONE] Map tab keeps the editor toolbox
      (ADD/DELETE/MOVE/ROUTE/REGION paint).
      `builder/src/builder/state/types.rs::MapTool` enumerates Select / AddSystem
      / DeleteSystem / MoveSystem / AddRoute / RegionPaint;
      `BuilderState::map_tool` holds the armed tool (default `Select`).
      `builder/src/builder/panels/map.rs::show_toolbox` renders the strip
      and Phase B click handlers branch on `state.map_tool`.

N4.   [DONE] Status bar: project path, dirty flag, validation status,
      pending-jobs spinner, derivation cache freshness.
      `builder/src/builder/panels/status.rs::show` writes project label,
      `dirty` flag, tri-coloured §V3 health pip, command cursor,
      derivation-cache entry count, and pending-job spinner.

N5.   Command palette (Ctrl-K): fuzzy-search every BuilderCommand by
      name.

================================================================================
42. PERFORMANCE TARGETS
================================================================================

PERF1. Single command apply: < 1 ms for sectors <= 500 systems.
PERF2. Map redraw (egui shapes): 60 FPS up to 500 systems + 2000 routes.
PERF3. Validation: < 50 ms for 500 systems.
PERF4. Invariants: < 50 ms for 500 systems.
PERF5. Cold derivation (all overlays): < 2 s for 500-system sector.
PERF6. Warm derivation (cache hits): < 50 ms.
PERF7. Search: 128 candidates evaluated in < 30 s on a 200-system project.

Use: `sort_unstable*`, `Vec::with_capacity`, `Arc<str>` clones (already
done), index pre-build, `mem::take` for vec rotations,
`rayon` only if profiling shows benefit.

================================================================================
43. IMPLEMENTATION PHASES (suggested order)
================================================================================

Item-range audit: every list below now spans the full ID range declared in
the matching section header (e.g. §3 Validation surface has V1..V6 in §33,
so Phase E carries V4..V6 — the items Phase A's V1..V3 omitted). U1..U2 stay
in Phase A and U3..U6 stay in Phase F by design (basic undo first, named
snapshots later). T1..T5 stay in Phase E and T6..T7 in Phase F by the same
split.

Phase A — Foundation
  D1..D6  - DONE  (§4)
  R1..R10 - DONE  (§1)
  P1..P6  - DONE  (§5)
  U1..U2  - DONE  (§38)
  V1..V3  - DONE  (§33; V4..V6 land in Phase E)
  N0..N4  - DONE  (§41; N5 command palette lands in Phase F)

Phase B — Direct entity edit
  G1..G6  - DONE  (§6 generation config — interactive panel + live
                   preview + apply-with-pinning + partial regen + preset
                   workspace; first Phase B workflow on top of the
                   Phase A foundation)
  S1..S6  - DONE  (§7 SYSTEM — hex map tools, full inspector, pinning,
                   bulk ops, single-system regen, coord-validity swap
                   dialog; backed by `BuilderCommand::SwapSystems` /
                   `ReplaceSystem`)
  W1..W7  - DONE  (§8 WORLD — full inspector over every `GeneratedWorld`
                   field, `*::VARIANTS`-backed enum pickers, side-table
                   pinning, single-world re-roll via
                   `BuilderState::regenerate_world` →
                   `generation::regenerate_world_payload`, searchable
                   features picker with weight preview, inline coupling
                   warnings, claims chip-row with deep-links)
  R1..R7  - DONE  (§9 routes — inspector, map add-route, manual distance
                   override, bulk operations, route-rules editor with live
                   preview scheduling, hidden-route builder, ensure-connected
                   bridge connector)
  F1..F7  - DONE  (§10 factions — FACTIONS panel: identity/kind/disposition/
                   weight inspector, preferred_* multi-pickers, hierarchical
                   roster grouped by top_faction > subfaction, style override
                   panel with colour pickers + glyph + border combo + live
                   preview tile, "Recompute style from kind" reset, presence
                   deep-link to CONTROL tab, F6 save via project_io::save_project,
                   legend-visibility tri-state stored on FactionDef as
                   optional `legend_visible`; new style override fields
                   `style_fill`/`style_accent`/`style_glyph`/`style_border`
                   round-trip cleanly to factions.toml; F5 presence editor
                   itself lands with §11 in Phase C)
  CL1..CL4 - DONE  (§12 claims — CONTROL tab `§CL1..§CL4` editor:
                   per-world chip-row, add-claim picker, Contested
                   auto-flag with aggregate header + filter, bulk
                   convert across the whole sector)
  C1..C8 - DONE  (§11 control + presence — CONTROL tab ships per-world
                   presence editor with influence-tier + intel_confidence +
                   10-dimension sliders, add/remove presence with roster
                   filter, dominance ComboBox with manual-lock side-table,
                   system control_state + primary_factions editor with
                   manual-override + recompute, live PowerProfile preview
                   with apply-to-rollups, and POWER PROJECTION /
                   INFLUENCE FIELD overlay toggle routed through the MAP
                   heatmap)

Phase C — Overlays editable
  REG1..REG7 - DONE (§14 regions — REGIONS panel ships per-region inspector
                     (id/name/kind/centre/hex list/remove), MAP-tab
                     RegionPaint brush (left=paint, right=erase) bound to
                     `BuilderState::selected_region_id`, "Grow seeded region"
                     command wrapping new `regions::seed_region`, live route
                     effect preview (perilous/degrade/upgrade counts + Apply
                     button calling `regions::apply_route_effects`),
                     `regions.toml` editor with enabled/count/mean_size/
                     apply_to_routes + ConditionEntry rows + Save passthrough,
                     §REG6 inline chips for REGION_HEX_OUT_OF_BOUNDS /
                     REGION_HEX_OVERLAP / REGION_ISOLATES_SECTOR, and an
                     ASCII §REG7 glyph preview (~ ^ = # * % + ?) sourced
                     from `RegionConditionKind::glyph`)
  SUB1..SUB5 - DONE (§13 subsectors — SUBSECTORS panel: §SUB1 cluster table
                     with label / name / capital / system count / dominant
                     faction; §SUB2 target_systems_per_subsector DragValue +
                     "Recluster" / "Reset target" buttons rebuilding the
                     `MapViewCache`; §SUB3 per-system "Move to..." picker with
                     `subsector_system_overrides` side-table reapplied after
                     every clustering pass, plus `subsector_manual` flag set
                     on touched cells; §SUB4 capital override ComboBox over
                     the cluster's member systems, stored in
                     `subsector_capital_overrides` and applied without
                     changing the subsector id; §SUB5 colour swatch defaulted
                     to FactionStyle fill of the controlling faction, custom
                     overrides in `subsector_colour_overrides`; "× clear all
                     overrides" button drops all four side-tables in one go;
                     MAP-tab `selected_subsector` now reads
                     `BuilderState::selected_subsector_id` so panel-side
                     selection lights up the live hex map)
  E1..E7  - DONE  (§15 economy — ECONOMY tab: per-world ResourceVector
                   override (§E1), per-world StrategicOutput override (§E2),
                   per-system tithe/supply/strategic_priority override (§E3),
                   stranded recompute + MAP red ring (§E4), `economy.toml`
                   editor + Save + Apply (§E5), lifeline-lane MAP highlight via
                   path_route_ids (§E6), trade/food/tithe/supply heatmap picker
                   feeding `SectorView::heatmap` (§E7); all mutations route
                   through `BuilderState::recompute_economy` and the MAP panel
                   reads `state.map_heatmap_mode` whenever §C7/§C8 are off)
  REL1..REL9 - DONE (§16 relations — RELATIONS tab: §REL1 diplomacy matrix
                     grid with click-to-select pair; §REL2 directional
                     a_to_b / b_to_a editors; §REL3 kind_rules editor;
                     §REL4 disposition_rules editor with -3..=3 delta;
                     §REL5 legacy pair_overrides list editor; §REL6 rich
                     RelationOverride editor pinning public/secret
                     attitudes, treaty, all 7 numeric dimensions,
                     directional overrides, and cause/reason; §REL7
                     feed_conflict toggle round-tripping through
                     RelationsMatrix; §REL8 [generation.relations].
                     min_world_presence slider feeding
                     derive_with_threshold; §REL9 Recompute matrix
                     button + auto-recompute on edit + Save
                     relations.toml passthrough via project_io::save_project)
  H1..H8 - DONE 
  I1..I5 - DONE
  AR1..AR3 - DONE
  O1..O2 - DONE (§31 orbital assets — per-system editor + blockade report
                 derive/override; panel `builder/src/builder/panels/orbital.rs`,
                 commands `SetOrbitalAssets` / `SetBlockadeReport`)
  SU1..SU2 - DONE (§32 surface regions — per-world editor + auto-seed
                   button; panel `builder/src/builder/panels/surface_regions.rs`,
                   command `SetSurfaceRegions`, `notes: String` added to
                   `SurfaceRegion`)
  CF1..CF6 - DONE (§28 conflict + stability — panel
                   `builder/src/builder/panels/conflict.rs`:
                   §CF1 per-world `ConflictState` editor (momentum / intensity /
                   mobilisation / attacker / defender / visible_controller /
                   started_tick / last_change_tick / age) routed through
                   `BuilderCommand::SetWorldConflict` with "Re-derive from
                   control" calling `conflict::derive_world_conflict`;
                   §CF2 per-system view aggregated via
                   `conflict::derive_system_conflict` by default, "Override
                   aggregate" toggle (`BuilderState::system_conflict_override`)
                   switches to direct edit through
                   `BuilderCommand::SetSystemConflict`;
                   §CF3 per-world `StabilityState` editor over all 7 dimensions
                   (`public_order`, `corruption`, `fear`, `rebellion_risk`,
                   `xenos_threat`, `warp_instability`,
                   `famine_or_resource_stress`) via
                   `BuilderCommand::SetWorldStability` with "Re-derive" calling
                   `stability::derive_world_stability`;
                   §CF4 "Advance N ticks" button driving
                   `BuilderState::advance_conflict_ticks` →
                   `BuilderCommand::AdvanceConflictTicks` (snapshots per-system
                   + per-world `ConflictState` + per-system `dominant` for an
                   exact revert; `HYSTERESIS_TICKS` preserved because the loop
                   calls `conflict::advance_sector` per tick);
                   §CF5 chronological tick log (`BuilderState::tick_log`,
                   bounded ring of `TickLogEntry` capped by
                   `tick_log_capacity`, in-memory only, filtered to the focused
                   system) populated automatically after every advance;
                   §CF6 `HeatmapMode::ConflictIntensity` added to
                   `src/heatmap.rs` (per-system `conflict.intensity` 0..=100,
                   labelled "CONFLICT", red tint) with a MAP toggle in the
                   SYSTEM panel that sets `BuilderState::map_heatmap_mode` so
                   the existing §E7 wiring through `SectorView::heatmap`
                   renders without extra plumbing.)

Phase D — Derivations + GM tools
  PER1..PER5 - DONE (§18 personae — panel
                     `builder/src/builder/panels/personae.rs`:
                     §PER1 per-faction-kind pool editor (name prefixes /
                     roots / suffixes / single names / titles / traits per
                     `KindPools`, custom-kind add, "Reset to defaults"
                     button) plumbed through
                     `BuilderState::data_catalogs.personae` ↔
                     `data/personae.toml`;
                     §PER2 per-anchor table + `[[manual]]` editor (manual
                     personae survive regenerate because
                     `personae::derive_with` appends `cfg.manual` last);
                     §PER3 "Auto-derive personae" button +
                     `personae_auto_recompute` toggle backed by
                     `BuilderState::recompute_personae` (added in
                     `state/derivations.rs`) which stashes the report on
                     `BuilderState::personae_report` (personae is overlay-
                     only, not on `GeneratedSector`);
                     §PER4 `min_world_dominance` + `max_per_world` +
                     `max_per_system` editor binding the matching
                     `PersonaeConfig` knobs;
                     §PER5 agenda derivation source surfaced as a row
                     tooltip — `kind / faction / anchor` lifted from
                     `personae::build_agenda`. Project I/O wired in
                     `project_io::catalogs_from_input` /
                     `save_project_as` / `reload_catalog` so personae.toml
                     round-trips alongside the other catalogs;
                     `synthesize_project_input` now feeds
                     `data_catalogs.personae` to validation/regeneration
                     instead of defaulting.)
  HK1..HK6 - DONE (§19 hooks — panel
                   `builder/src/builder/panels/hooks.rs`:
                   §HK1 ranked hook list with per-`HookKind` ComboBox
                   filter (`show_filter_row`) over the cached
                   `BuilderState::hooks_report` published by
                   `BuilderState::recompute_hooks`; rows are sorted
                   descending by dramatic weight inside
                   `hooks::derive_with`;
                   §HK2 detail card driven by
                   `BuilderState::hooks_edit_target` /
                   `selected_hook_id` showing id / kind / anchor /
                   weight / gm-only / situation / stakes / factions /
                   complications;
                   §HK3 manual entry editor on `HooksConfig::manual`
                   with per-entry kind picker, anchor-scope toggle
                   (System / World / Route), id editors, prose and
                   complications fields;
                   §HK4 "Regenerate hooks" button calling
                   `BuilderState::recompute_hooks` — manual hooks
                   survive because `hooks::derive_with` drops any
                   derived hook sharing a manual id and appends the
                   whole `cfg.manual` block last (auto-recompute toggle
                   mirrors the personae pattern);
                   §HK5 `BuilderState::hooks_player_edition` checkbox
                   that flips `HooksConfig::hide_hidden_hooks` on every
                   recompute so the cached report already has
                   `gm_only = true` rows stripped;
                   §HK6 every anchor cell + "highlight on map" button
                   funnels through `focus_anchor` →
                   `BuilderState::focus_entity` with
                   `EntityRef::System` / `World` / `Route` (falls back
                   to `EntityRef::Tab(BuilderTab::Map)` when the
                   anchor id is empty). Project I/O wired in
                   `project_io::catalogs_from_input` /
                   `save_project_as` / `reload_catalog` so
                   `data/hooks.toml` round-trips alongside the other
                   catalogs; `[inputs].hooks` added to
                   `src/config.rs::InputConfig` and `src/input.rs`
                   parses the file into `ProjectInput::hooks`;
                   `synthesize_project_input` now feeds
                   `data_catalogs.hooks` to validation / regeneration
                   instead of defaulting.)
  ST1..ST4 - DONE (§20 sites — panel
                   `builder/src/builder/panels/sites.rs`:
                   §ST1 per-world site editor with the full 21-kind
                   `SiteKind` ComboBox over the cached
                   `BuilderState::sites_report` published by
                   `BuilderState::recompute_sites`; row click sets
                   `selected_site_id` / `sites_edit_target` and focuses
                   `EntityRef::World { system, world }` so cross-tab
                   links land first-class;
                   §ST2 "Auto-derive sites" button +
                   `sites_auto_recompute` toggle backed by
                   `BuilderState::recompute_sites` (added in
                   `state/derivations.rs`) which runs
                   `sites::derive_with` — manual entries survive
                   because the derivation appends `cfg.manual` after
                   the derived rows;
                   §ST3 `BuilderState::sites_player_edition` checkbox
                   that flips `SitesConfig::player_edition` on every
                   recompute so derived rows where
                   `public_status != actual_status` are stripped, and
                   the detail card hides the `actual_status` column;
                   §ST4 `show_config_section` binds
                   `max_per_world` / `skip_uninhabited`;
                   `show_manual_editor` edits `SitesConfig::manual`
                   with id / kind / system-world ids / name /
                   controller / public + actual status / known-to +
                   tags CSV / hook fields. Project I/O wired in
                   `project_io::catalogs_from_input` /
                   `save_project_as` / `reload_catalog` so
                   `data/sites.toml` round-trips alongside the other
                   catalogs; `synthesize_project_input` now feeds
                   `data_catalogs.sites` to validation / regeneration
                   instead of defaulting.)
  M1..M5 - DONE (§21 missions — panel
                   `builder/src/builder/panels/missions.rs`:
                   §M1 mission-list panel over the cached
                   `BuilderState::missions_report` published by
                   `BuilderState::recompute_missions` (added in
                   `state/derivations.rs`); rows + side detail card
                   cover kind / patron / target / primary + secondary
                   location / scale / visibility / objective / hidden
                   complication / reward / consequence;
                   §M2 "+ manual mission" form edits
                   `MissionsConfig::manual` with kind / patron / target
                   / location / route-ids / scale / visibility / weight
                   / GM-only knobs;
                   §M3 "Auto-derive missions" button +
                   `missions_auto_recompute` toggle backed by
                   `BuilderState::recompute_missions` which runs
                   `missions::derive_with` — manual entries survive
                   because the derivation extends with `cfg.manual` after
                   the per-anchor cap;
                   §M4 `BuilderState::missions_player_edition` checkbox
                   that flips `MissionsConfig::player_edition` on every
                   recompute so `gm_only` rows are stripped and the
                   panel hides the GM column / hidden-complication row;
                   §M5 row + detail-card "highlight" buttons run
                   `focus_primary_location` which parses
                   `primary_location` (`sys` or `sys/world`) and calls
                   `BuilderState::focus_entity` with
                   `EntityRef::System` / `World` / `Route` (falls back
                   to the first route id, then `EntityRef::Tab(Map)`,
                   when the anchor string is empty). Project I/O wired
                   in `project_io::catalogs_from_input` /
                   `save_project_as` / `reload_catalog` so
                   `data/missions.toml` round-trips alongside the other
                   catalogs; `[inputs].missions` added to
                   `src/config.rs::InputConfig` and `src/input.rs` parses
                   the file into `ProjectInput::missions`;
                   `synthesize_project_input` now feeds
                   `data_catalogs.missions` to validation / regeneration
                   instead of defaulting.)
  PR1..PR4 - DONE (§22 prose — panel
                   `builder/src/builder/panels/prose.rs`:
                   §PR1 per-system editor over the cached
                   `BuilderState::prose_report` published by
                   `BuilderState::recompute_prose` (added in
                   `state/derivations.rs`); system picker (`ComboBox`
                   seeded from `selected_prose_system_id`, falling
                   through to `selected_system_id` on first focus)
                   plus an "Override" checkbox that flips
                   `ProseConfig::overrides::systems`. The first toggle
                   seeds the override with
                   `entry.paragraphs.join("\n\n")` so the user edits in
                   place; "Revert to derived" removes the entry. The
                   derived paragraphs stay cached on
                   `SystemProse::derived_paragraphs` so the "Derived
                   paragraphs (read-only)" collapsing block can restore
                   them without re-running derivation;
                   §PR2 `show_overview_editor` mirrors §PR1 against
                   `ProseConfig::overrides::overview`;
                   `ProseReport::overview_is_override` drives the
                   "AUTHORED" badge. Blank / whitespace-only overrides
                   fall back to the derived overview at derive time;
                   §PR3 `show_tone_section` exposes a `ComboBox` over
                   `ProseTone::Gazetteer` / `ProseTone::Dispatch` bound
                   to `ProseConfig::tone`, plus include-overview /
                   include-per-system checkboxes that mirror the CLI
                   knobs;
                   §PR4 "Regenerate prose" runs
                   `BuilderState::recompute_prose` which calls
                   `prose::derive_with`. Manual overrides survive
                   because the derivation re-applies the
                   `ProseOverrides` block after the deterministic
                   derivation pass. A `prose_auto_recompute` toggle
                   (default `true`) re-runs the derivation on every
                   catalog edit through `on_catalog_edited`. Project
                   I/O wired in `project_io::catalogs_from_input` /
                   `save_project_as` / `reload_catalog` so
                   `data/prose.toml` round-trips alongside the other
                   catalogs; `[inputs].prose` added to
                   `src/config.rs::InputConfig` and `src/input.rs`
                   parses the file into `ProjectInput::prose`;
                   `synthesize_project_input` now feeds
                   `data_catalogs.prose` to validation / regeneration
                   instead of defaulting. The `sectorforge prose` CLI
                   also honours `[inputs].prose` when `--project` is
                   supplied so authored overrides survive on the CLI
                   path.)
  BR1..BR5,   (§23 briefing — DONE.
                   `builder/src/builder/panels/briefing.rs` ships the
                   preset / observer / min-confidence controls plus the
                   `briefing::apply` preview and the `rfd`
                   folder-picker-backed `sectorforge::write_briefing`
                   export. BuilderState gained
                   `briefing_preset` /  `briefing_observer` /
                   `briefing_min_confidence` /  `briefing_preview_md` /
                   `briefing_preview_pack` /  `briefing_export_dir` to
                   carry the panel's selection plus the cached pack
                   between generate and export so the same redacted
                   pack the GM previewed is the one the writer emits.)
  INT1..INT4. (§24 interestingness — DONE.
                   `builder/src/builder/panels/interestingness.rs` ships
                   the §INT1 profile picker, §INT2 "Score sector" button +
                   strengths/weaknesses readout, §INT3 per-metric band
                   chart rendered through `ui.painter_at`, and §INT4
                   per-profile threshold override editor. BuilderState
                   gained `interestingness_profile`,
                   `interestingness_report`,
                   `interestingness_custom_overrides` (keyed by snake-case
                   profile id), and `interestingness_custom_pick` so the
                   profile, cached scorecard, per-profile override tables,
                   and the "Add override" scratch selection survive
                   between Score-sector clicks. The cached report is
                   cleared on any profile / override mutation so the chart
                   never paints stale bands.)

Phase E — Tooling
  SR1..SR5,   (§26 search)
  DF1..DF5,   (§27 diff)
  A1..A4,     (§25 analytics — was "1..A4" typo)
  SG1..SG5,   (§36 segmentum)
  PF1..PF6,   (§37 project + files)
  EX1..EX8,   (§34 export bundle)
  T1..T5,     (§40 testing — T6..T7 land in Phase F)
  V4..V6,     (§33 strict mode, invariant catalogue, refuse-on-error gate)
  LD1..LD4.   (§39 live derivations)

Phase F — Polish
  U3..U6,        (§38 snapshots)
  T6..T7,        (§40 smoke + bench)
  PERF1..PERF7,  (§42)
  N5,            (§41 command palette — Ctrl-K)
  keyboard shortcuts (beyond the §U2 Ctrl-Z/Y already wired),
  T1..T5 of §35 theme parity,
  accessibility.

================================================================================
44. OPEN QUESTIONS (resolve before Phase A locks)
================================================================================

Q1.  [ANSWERED] `pinned` field on systems/worlds — extend `GeneratedSector` schema
     (breaks JSON compat) or store in a side-table inside
     `BuilderState` (no schema break, lost on export)?
     -> BUILDERSTATE SIDE-TABLE. Store in `pinned_systems` and `pinned_worlds`
     inside `BuilderState`. This avoids breaking `GeneratedSector` JSON
     compatibility. Manual edits survive within the session but are not
     exported to raw JSON. Update D5, S3, W3.

Q2.  [ANSWERED] Stable IDs on rename: tombstone table (compat) vs.
     renumber-on-save (simpler)?
     -> COMPAT (Tombstone). Done in §49. Uses `id_history` in `GeneratedSector`.

Q3.  [ANSWERED] Personae/site config: add `[inputs].personae` + `[inputs].sites`
     entries to `sectorforge.toml`, or keep purely derived?
     -> ADD ENTRIES. Done in §46. Manual entries merged into derivations.

Q4.  [ANSWERED] Off-thread runner: ad-hoc `thread::spawn` + channels, or single
     `tokio` runtime? Recommend ad-hoc — no async surface elsewhere.
     -> AD-HOC. Done in §47. Uses `std::thread::spawn` and `std::sync::mpsc`.

Q5.  [ANSWERED] Live preview during generation config edits: full regenerate (slow)
     vs. incremental (hard to make correct)? Recommend debounced full
     regenerate into scratch sector, swap on apply.
     -> DEBOUNCED FULL. Done in §48. 200ms debounce, off-thread gen.

Q6.  [ANSWERED] `.sgforge` session file format: tar (portable but external lib)
     vs. JSON envelope of base64-encoded files (no new dep)?
     -> JSON ENVELOPE. Support JSON primarily (base64-encoded inner files),
     but allow a tar option that can be toggled on if the dependency is
     present. Update D6.

================================================================================
45. WORLDS DATA NATIVE-FIRST (CSV path fully removed)
================================================================================

Rationale: builder pivots to in-app construction. The previous twin-CSV
(`key.csv` + `generator.csv`) was a spreadsheet-import shape — stringly
typed, redundant with the existing enums, no structure for feature
pools, awkward to edit via typed builder widgets. Native model =
typed serde TOML. CSV support — loader, writer, old GUI editor, conversion
command, and `OutputFormat::Csv` — has been removed entirely.

WD1. [DONE] Enum-authoritative variants. Each enum in
     `src/worlds.rs` (StarColour, WorldType, Atmosphere, Temperature,
     Biosphere, Population, TechLevel, Government, NotableFeature)
     exposes `pub const VARIANTS: &'static [Self]`. `KeyTables`
     uses `KeyTables::from_enums()` exclusively to build the full
     table from compiled variants.

WD2. [DONE] Typed `worlds.toml` loader. New file
     `src/worlds_toml.rs`. Defines `WorldsConfig { generation:
     Vec<GenerationRow>, features: FeaturePoolConfig }` with serde
     `Serialize`/`Deserialize`. Loader returns the same
     `(KeyTables, Vec<GenerationRow>)` tuple downstream code expects
     (`world_pool::build_pool` untouched). Sole supported format.

WD3. [DONE] Structured feature pools. `FeaturePoolConfig` carries
     `global`, `by_world_type: BTreeMap<WorldType, Vec<(NotableFeature,
     f64)>>`, `by_star_colour: BTreeMap<StarColour, Vec<...>>`. Maps
     into existing `FeaturePool` at pool-build time. Round-trips
     deterministically (BTreeMap ordering).

WD4. [DONE] Native typed editing foundation. The existing viewer/editor
     `viewer/src/data_editor.rs` reads/writes `WorldsConfig` exclusively;
     rows are edited via enum dropdowns (`ComboBox` over `Enum::VARIANTS`)
     and `DragValue<f64>` weights. Builder parity belongs in
     `sectorforge-builder` via PF3 / `builder/src/builder/panels/*`; do not
     route builder world-data editing through `sectorforge-viewer`.

WD5. [DONE] CSV removal. Twin-CSV loader, `convert-csv-to-toml`
     CLI/library entry point, old GUI CSV editor view, CSV writers in
     `src/export.rs` and `src/economy.rs`, and `OutputFormat::Csv`
     have all been removed. All bundled examples
     (`examples/{big_test,big_sparse_test,llm_test,m42_project}`)
     and the `_base` preset ship `worlds.toml` as the sole source
     of truth.

================================================================================
46. PERSONAE & SITES IMPORTS (Q3 Answered)
================================================================================

Rationale: Parity with history events. Users need to inject handcrafted
major NPCs and planetary locations that survive regeneration. The builder
manages these via manual entries in the config files, which the derivation
logic merges into the procedurally generated set.

PSI1. [DONE] Extended InputConfig. AppConfig now supports optional
      'personae' and 'sites' strings in the [inputs] table.

PSI2. [DONE] Manual Entry Schema. 'PersonaeConfig' and 'SitesConfig'
      now include a 'manual' Vec of the respective entity types.

PSI3. [DONE] Merge Logic. 'derive_personae_with' and 'derive_sites_with'
      append manual entries to the generated list before returning the
      report. Verified with 'tests/imports_test.rs'.

PSI4. [DONE] Project Loader Parity. 'load_project' reads personae.toml
      and sites.toml if referenced, calculating digests for the manifest.

================================================================================
47. OFF-THREAD RUNNER (Q4 Answered)
================================================================================

Rationale: Keep the UI responsive during heavy computation (generation,
search, complex derivations). Ad-hoc threads avoid the overhead and
complexity of an async runtime when all library code is synchronous.

OTR1. [DONE] Job Infrastructure. 'gui-core/src/jobs.rs' defines 'JobHandle<T>'
      and 'JobContext'. Uses 'std::thread::spawn' for execution.

OTR2. [DONE] Communication. Uses 'std::sync::mpsc' channels for returning
      results and 'Arc<Mutex<f32>>' for shared progress tracking.

OTR3. [DONE] UI Integration. 'JobContext' holds an 'egui::Context' and calls
      'request_repaint()' when progress updates, ensuring the UI stays
      fresh.

OTR4. [DONE] Cancellation. 'JobHandle' includes an 'Arc<AtomicBool>' that
      worker threads can poll to support clean cancellation of long jobs.

OTR5. [IN PROGRESS] Lock-free progress + handle-stub unification. Tracked in
      IMPROVEMENT.txt §P1-3 (rename `builder::state::JobHandle` ->
      `JobSummary`, drop unused `pending_jobs` or wire to real handles) and
      §P2-4 (swap `Arc<Mutex<f32>>` -> `Arc<AtomicU32>` bit-cast, downgrade
      cancel `Ordering::SeqCst` to Acquire/Release, rate-limit
      `ctx.request_repaint()` from `set_progress`). Does not change OTR1..OTR4
      external contract.

================================================================================
48. LIVE PREVIEW (Q5 Answered)
================================================================================

Rationale: Users need immediate feedback on generation config changes (seed,
density, etc.) without losing their current working sector until they are
ready. Debounced off-thread regeneration ensures the UI stays fluid.

LPV1. [DONE] Debounce Logic. `builder/src/builder/preview.rs::PreviewState`
      includes the debounce timer. Edits reset the timer (200ms). The
      `sectorforge-builder` UI loop spawns the job only after the timer
      expires.

LPV2. [DONE] Scratch Sector. `PreviewState` holds the scratch
      `GeneratedSector`. Background jobs write into this scratch space,
      keeping `BuilderState::sector` untouched until apply.

LPV3. [DONE] Preview UI. The `sectorforge-builder` generation panel shows
      preview readiness/counts and an "APPLY PREVIEW" button to commit the
      scratch sector. Shared rendering helpers may come from
      `sectorforge-gui-core`, but the preview state lives in the builder app.

LPV4. [DONE] Worker Integration. Uses the §47 Job infrastructure to
      run 'sectorforge::generation::generate' without blocking the UI thread.

================================================================================
49. STABLE IDS & TOMBSTONES (Q2 Answered)
================================================================================

Rationale: External references (segmentum manifests, history, manual notes)
break if IDs are aggressively renumbered. The "compat" mode ensures that
renaming or moving entities preserves their identity.

SID1. [DONE] Tombstone Table. 'GeneratedSector' includes an 'id_history'
      BTreeMap mapping original IDs to current IDs.

SID2. [DONE] Re-index Pass. 'GeneratedSector::reindex_ids' supports two modes:
      'sequential' (fills holes, renumbers everything) and 'stable' (preserves
      existing IDs, only assigns to new entities).

SID3. [DONE] Mutation Integration. The core model now resides in
      'src/sector_model/mod.rs' with structural re-indexing in
      'src/sector_model/mutation.rs'.

SID4. [DONE] Builder Setting. `BuilderState` includes `stable_ids_on_rename`
      (default true) to control re-indexing behavior during
      `sectorforge-builder` sessions.

================================================================================
50. CROSS-TAB NAVIGATION (§LINK)
================================================================================

Rationale: Builder UI surfaces dozens of entity ids (factions, systems,
worlds, routes, regions, subsectors, history events) across 24 tabs. Users
need to traverse those references with one click and a back-stack rather
than hunting through pickers. Phase A..C grew ~20 ad-hoc `ui.link` sites
that all reimplement the same `state.active_tab = X; state.selected_*_id =
Some(y)` pattern. §LINK centralises this surface and closes the remaining
gaps.

LINK1. [DONE] EntityRef enum. New file builder/src/builder/state/nav.rs
       defines `EntityRef` covering System, World, Faction, Route, Region,
       Subsector, Persona, HistoryEvent, Hook, and Tab variants. `Persona`
       and `Hook` are stub-compatible — the variants exist before the
       Phase D panels land.

LINK2. [DONE] focus_entity dispatch. `BuilderState::focus_entity(target)`
       in builder/src/builder/state/selection.rs is the only path that
       writes `active_tab`. Sets `active_tab = target.target_tab()` and
       the matching selection field. Idempotent. Pushes prior focus onto
       `nav_back_stack`, clears `nav_forward_stack`.

LINK3. [DONE] Back-stack. `BuilderState` carries `nav_back_stack` and
       `nav_forward_stack: Vec<EntityRef>`, each capped at 64. Alt+← /
       Alt+→ navigate; tab strip exposes chevron affordances; stacks are
       in-memory only and do not survive reload.

LINK4. [DONE] Entity link widget. `gui_core::entity_link(ui, label,
       with_arrow) -> Response` is the single rendering helper. All 20
       existing ad-hoc link sites refactor to use it. No `state.active_tab =`
       assignment is permitted outside the tab-strip click handlers in
       builder/src/builder/panels/nav.rs.

LINK5. [DONE] Gap closure. Converted plain-text entity references to
       `entity_link` in: world.rs claims chips, control.rs presence editor,
       economy.rs stranded badges + lifeline endpoints. Route editor combos
       keep the existing dual-widget pattern (combo edits draft, trailing
       `→` navigates).

LINK6. [DONE] Phase D/E pre-wiring. PERSONAE / HOOKS / BRIEFING panels
       remain stubs, but inbound `EntityRef::Persona(_)` /
       `EntityRef::Hook(_)` dispatches already select the right id on the
       stub so that linkification lands first-class when the panel ships.

LINK7. [DONE] Tests. `builder/src/builder/state/tests.rs` covers every
       EntityRef variant, back/forward symmetry, idempotency, stack cap,
       forward-stack clearing on new focus, and world-ref dual-selection.

================================================================================
END OF BUILDER_REQS.txt
